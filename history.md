# 작업 기록

코드를 읽으면 알 수 있는 것은 적지 않는다. **왜 그렇게 했는지**, **어디서 넘어졌는지**,
**아직 안 한 것**만 남긴다. 새 작업을 시작할 때 이 파일부터 읽으면 코드를 다시 훑지 않아도 된다.

작업이 끝나면 이 파일도 같이 고친다.
**앱에 보이는 게 바뀌었으면 `src/lib/updates.ts` 의 업데이트 내역도 같이 적는다** — 아래 "업데이트 내역 적기" 참고.

---

## 앱 한눈에

Next.js 16 · React 19 · Tailwind 3 · Firebase(Auth + Firestore). 전부 클라이언트 렌더(`ssr: false`).
사용자는 한 명, 아이폰에서 매일 쓴다.

**탭 여섯 개** — 캘린더 · 강의 · 시간표 · 토익 · 판다 · **이벤트(한시적)**
탭 안에서 또 나뉘는 게 하나 있다 — 시간표(시간표/타이머)
**이벤트는 윷놀이다. 기간이 끝나면 통째로 들어낸다** (아래 "윷놀이 이벤트")

| 파일 | 하는 일 |
|---|---|
| `lib/config.ts` | 시간표·학기. **하드코딩이라 학기마다 코드를 고쳐야 한다** |
| `lib/store.ts` | Firestore 구독·쓰기 훅 전부 |
| `lib/date.ts` | 날짜·주차 계산. `displayWeek` 은 캘린더와 강의 탭이 같이 쓴다 |
| `lib/sprites.ts` | 도트 그림. 글자 배열이라 이미지 파일이 아니다 |
| `lib/pet.ts` | 판다 방 크기 · 포인트 상수 |
| `shop/` | 상점의 모든 아이템 · 벽지 · 바닥. 아이템 하나 = 파일 하나 (아래 "상점 파일") |
| `lib/timer.ts` | 하루짜리 스톱워치. 유일하게 Firestore 가 아니라 localStorage 를 쓴다 |
| `components/PetRoom.tsx` | 방 그리기 |
| `components/PetView.tsx` | 방 + 상점 |
| `components/PandaView.tsx` | 스티커 / 키우기 나누는 껍데기 |
| `components/TimeView.tsx` | 시간표 / 타이머 나누는 껍데기 |
| `components/TimerView.tsx` | 화면을 덮는 스톱워치. 눕혀서 쓴다 |
| `components/ClockFace.tsx` | 7세그먼트 시계 숫자. **도트가 아니라** 육각형 |
| `components/GuidePopup.tsx` | 헤더 왼쪽 위 `가이드`. 포인트 얻기 / 업데이트 내역 두 쪽 |
| `lib/updates.ts` | 업데이트 내역 글. **작업할 때마다 여기 적는다** |
| `components/Splash.tsx` | 불러오는 동안 보이는 화면 (토끼 · 잠깐만 · 당근 게이지). **Firebase 를 안 건드린다** |
| `lib/yut.ts` · `EventView` · `YutBoard` · `YutThrow` | 윷놀이 이벤트. **한시적 — 끝나면 지운다** |

**Firestore**

```
users/{uid}/events/{id}            학교 일정
users/{uid}/tasks/{id}             강의 · 과제 · 할일
users/{uid}/words/{id}             토익 단어
users/{uid}/stickers/{YYYY-MM}     { days: number[] }
users/{uid}/pet/state              판다 방 (문서 하나). spots 에 가구 자리까지 들어간다
users/{uid}/event/yut              윷놀이 판 (문서 하나). 한시적
```

보안 규칙은 `users/{uid}/**` 통째로 열려 있어서 컬렉션을 늘려도 규칙은 안 건드려도 된다.

---

## 판다 키우기

### 방 — 세 면

**벽을 세 장 그리는 게 아니다.** 열(세로 한 줄)마다 바닥이 시작하는 높이를 다르게 준 것이고,
`floorTopAt(x)` 하나가 **벽·바닥·걸레받이·판다가 다닐 곳을 전부** 정한다.
`floorLeftAt(y)` / `floorRightAt(y)` 는 그걸 거꾸로 푼 것이라 늘 맞물린다 —
따로 계산하지 말 것.

```
방        80 x 92 칸   (ROOM.w / h)
옆벽      양쪽 12 칸   (ROOM.side) — 가운데 56칸이 뒷벽
걸레받이   뒷벽은 54 줄, 옆벽은 앞으로 올수록 내려온다 (계단 모양)
바닥      뒤 56칸 → 앞 80칸 사다리꼴
판다      16 x 16, 처음 (30, 62). 그다음부터는 스스로 돌아다닌다
창문      24 x 22, 뒷벽 한가운데 (28, 10)
```

폰(가로 393px)에서 한 칸이 4.0px, 방 높이가 366px.
**스크롤 없이 한 화면에 들어가야 한다.** 예전에 68줄이었을 때 사파리 주소창이 있으면
33px 넘쳐서 상점 버튼이 안 보였다. 방을 키우고 싶어지면 이 제약부터 확인할 것.

48 x 57 칸이던 것을 80 x 92 로 넓힌 것이라 **그림은 하나도 안 고쳤다.** 칸이 촘촘해지니
같은 도트가 저절로 작아졌다 (판다 114px → 68px). 더 줄이는 건 112 x 128 이 한계다 —
판다 눈이 2 x 2 칸이라 그보다 촘촘해지면 눈이 5px 아래로 내려가 표정이 뭉개진다.

### 판다 산책

CSS 키프레임으로 좌우를 오가던 것을 **PetRoom 이 0.11초마다 한 칸씩 직접 옮기는** 방식으로 바꿨다.
바닥 안에서 갈 곳을 스스로 정하고, 도착하면 0.9~3.5초 쉬었다 또 정한다.

- **한 칸씩 정수로만** 움직인다. 반 칸이 나오면 도트가 어긋나 깨져 보인다 (아래 `steps()` 항목과 같은 이유)
- **뒤로 물러설수록 다닐 폭이 좁아진다.** 매 걸음 `floorLeftAt/RightAt` 으로 다시 가둔다
- 가는 쪽을 보고 `scale(-1,1)` 로 몸을 뒤집는다. 정수 배율이라 도트가 안 뭉갠다
- **옮기는 동안에는 세워둔다.** 손가락 밑에서 돌아다니면 가구를 집기 어렵다

### 화면 짜임 — 판다 탭

판다 탭은 **키우기 화면 하나**다 (9/21). 맨 위 조작줄 한 줄, 그 아래 방, 끝.

```
[스티커] [상점] [옮기기]   PetBar.tsx — 방 위에 아무것도 안 얹는다
┌──────────────┐
│      방      │          PetRoom
└──────────────┘
끌어서 옮기고 ↺ ↻ 로 …    옮기기 중에만, 방 **아래** 한 줄
```

- 9/21 전에는 스티커 / 키우기 두 쪽으로 나뉘고, 상점 · 옮기기가 **방 위에 동그랗게 떠 있었다.**
  그 버튼들이 앞바닥 두 모서리를 가려서 **그 자리에 가구를 못 놓는다**는 말을 들었다 (자리를 옮기자는 것도 사용자 제안).
  쪽을 나누던 줄이 그대로 조작줄이 됐으므로 **세로 공간은 한 칸도 안 늘었다** — "스크롤 없이 한 화면" 제약을 안 건드린다
- **스티커는 큰 팝업**(`Popup wide`). 하루 한 번 눌렀다 닫는 일이라 쪽보다 팝업이 맞다.
  방을 아직 못 불러왔을 때도 조작줄은 먼저 세워서 스티커는 붙일 수 있게 한다
- 셋 중 **옮기기만 켜지는 것**이라 분홍으로 남는다. 나머지 둘은 여는 것이라 눌림 표시가 없다.
  글자만 늘어놓으면 탭으로 보여서 앞에 작은 그림을 하나씩 붙였다
- 방 위에 남는 건 **돌리기 화살표 ↺ ↻ 뿐이다** — 집은 가구 옆에 붙어야 해서 어쩔 수 없다

### 가구 배치

소품마다 자리가 코드에 박혀 있던 것을 **놓은 자리를 저장하는** 방식으로 바꿨다.
"왼쪽 바닥·오른쪽 바닥" 한 칸씩 쓰던 슬롯도 없앴다 — 가진 걸 다 꺼내놓을 수 있다.

- **옮기기** 버튼을 누르면 가구마다 집는 자리가 생기고, 끌어서 옮긴다
- 손을 떼면 **손이 간 칸 그대로** 놓인다. 9/21 전에는 두 칸 격자(`SNAP`) 에 붙였는데,
  그러면 **홀수 칸에 못 놓는다** — 한 칸 밀면 반올림이 제자리로 되돌리고 어떤 칸은 두 칸 튀었다.
  "손가락으로 한 칸은 못 맞춘다" 고 넣은 규칙이었지만, 안 움직이는 쪽이 훨씬 답답하다
- 저장은 **손을 뗐을 때 한 번만** 한다. 끌고 있는 동안 쓰면 Firestore 쓰기가 폭발한다
- `fit()` 이 방 밖으로 못 나가게 한다. 바닥 것은 **발끝이 놓인 줄**에 따라 좌우 끝이 달라진다
- **벽선(`floorTop`) 이 네 칸(slot) 의 공통 한계다** — 여기에 닿으면 "벽에 딱 붙은" 자리다
  | 칸 | 벽 쪽 한계 | 왜 |
  |---|---|---|
  | `floor` 가구 · 인형 | 발끝이 벽선에 닿을 때까지 | 뒷벽에 붙여 세운 모습 |
  | `flat` 러그 · 방석 | 윗줄이 벽선까지 (그 위로는 못 간다) | 깔개가 벽을 타고 올라가면 걸어둔 그림이 된다 |
  | `wall` 창문 · 액자 · 청사초롱 | 아랫줄이 벽선에 닿을 때까지 | 창호문처럼 바닥까지 내려오는 것이 있다 |
  | `top` 화분 · 책 · 꽃병 | **제일 큰 가구를 벽에 붙였을 때의 윗면**(`topReach()`) | 가구가 벽까지 가면 그 위에도 얹혀야 한다 |
  - `top` 의 한계만 고정값이 아니라 **그림에서 잰다.** 더 큰 가구가 들어오면 따라 올라간다

### 그리는 순서

SVG 에는 z-index 가 없다. 그리는 순서가 곧 순서다. 지금은 이렇게 쌓는다:

```
벽 · 바닥 · 걸레받이
flat  (러그) — 늘 맨 아래. 판다가 그 위를 밟고 지나간다
wall  (창문 · 액자 · 벽시계) — 늘 판다 뒤
floor (판다보다 뒤) → top (판다보다 뒤)
판다
floor (판다보다 앞) → top (판다보다 앞)
집는 자리 — 옮기는 동안만, 맨 위에 따로
```

**`top`(가구 위에 얹는 것) 이 왜 따로인가** — 책상 위 화분은 발끝이 책상보다 높아서(뒤라서)
발끝 순으로만 정렬하면 **책상 뒤로 숨는다.** 그래서 같은 차례 안에서 바닥 가구보다 나중에 그린다.
앞을 지나가는 판다에는 그대로 가려진다.

집는 자리를 맨 위에 따로 얹는 건 **소품끼리 겹쳐 있어도 집을 수 있어야** 해서다.

### 정해진 규칙

- **판다는 아기로 고정.** 크면 덜 귀여워서 성장 단계는 안 만든다. 대신 방과 옷이 늘어난다
- **창문은 하나만 걸린다.** 일곱 종이 다 같은 창문 자리라서다 (`only: "win"`).
  그 밖의 소품은 갯수 제한이 없다 — 가진 걸 다 꺼내놔도 된다
- **포인트는 하는 일마다 조금씩 준다.** 값을 한 군데 몰면 체크(5초) 와 공부(3시간) 가 같아진다.
  값은 전부 `pet.ts` 맨 아래 상수 하나씩이고, 셈하는 곳은 전부 그 상수를 본다

  | 어디서 | 상수 | 얼마 | 조건 | 되돌아가는 경우 |
  |---|---|---|---|---|
  | 스티커 | `PER_STICKER` | 100 | | 떼면 |
  | 스티커 10개 | `BONUS` | +500 | **그 달** 10개마다 | 떼서 10개 밑으로 내려가면 |
  | 스티커 연속 | `STREAK_BONUS` | +50 | **이어 붙인 날** 7일마다. 달을 넘는 연속은 안 센다 | 중간을 떼서 끊기면 |
  | 강의·과제 체크 | `PER_TASK` | 20 | **하루 다섯 번**까지(`TASK_CAP`). 넘기면 체크는 되고 점수만 0 | 체크를 풀거나, 체크된 걸 지우면 (준 만큼만) |
  | 토익 퀴즈 | `PER_QUIZ` | 문제당 10 (0~50) | **그날 처음 끝낸 한 판만**. 틀려도 기회가 끝난다(다시 풀어 만점 노리기 막기). 단어가 모자라 5문제가 안 되는 판·중간에 그만둔 판은 기회를 안 쓴다 | 없음 |
  | 타이머 | `PER_FOCUS` | 15 | **일시정지 없이 25분**마다 · **하루 네 번**까지 | 없음 |

  **9/17 스티커 25→100 · 10개 보너스 100→500 으로 올렸다 (사용자 요청).** 부지런한 날이 180 → **약 300점**
  (스티커 100 + 10개 보너스 하루 50 + 연속 7 + 퀴즈 50 + 타이머 60 + 과제 28) — 스티커가 벌이의 절반이다. 아래는 올리기 전 셈.
  **하루에 받을 수 있는 양 — "부지런한 날 180점"** (9/17 사용자가 정했다. 상점이 16,340점으로 커져서 올렸다)
  스티커 25 + 보너스를 하루로 나눈 17쯤(10개 보너스 ≈ 10, 연속 ≈ 7) + 퀴즈 50 + 타이머 네 번 60 + 과제(주 10개면 하루 28쯤) ≈ **180**.
  보통 날(퀴즈 주 5번 · 타이머 두 번) 은 135점쯤이라 **넉 달이면 상점을 다 산다** — 한 학기에 맞췄다.
  올릴 때 체크(20) 는 그대로 두고 **공부 쪽(퀴즈 · 타이머) 을 더 올렸다** — 애쓴 것에 값을 준다는 원래 방향.
  과제를 하루 한도(5번) 까지 채우면 이론상 250점쯤도 된다. 이전 값: 스티커 15 · 퀴즈 30 · 타이머 10
  속도를 바꾸려면 상수만 만진다
- **처음엔 완전히 비어 있다.** 민무늬 벽(`w0`), 맨바닥(`f0`), 아무것도 안 걸친 판다
- **값은 50 ~ 1,000점, 전부 26,900점** (소품 50 · 벽지 10 · 바닥 9, 기본 벽·바닥 제외).
  **일반 42개(~499) · 골드 22개(500~999) · 프리미엄 5개(1,000)** — 등급은 아래 "상점 등급".
  9/17 조선 세트(7,800점) 가 들어와 19,100 → 26,900. **"넉 달" 이 깨졌다** — 보통 날 135점이면 6개월 반, 부지런한 날 180점이면 5개월.
  포인트 값은 아직 안 만졌다 (사용자에게 물어볼 것)
  리본이 제일 싼 건 며칠이면 첫 구매가 되게 하려는 것. **한 학기에 다 안 채워지게** 잡은 값이다.
  비싼 것이 붙어 옛 값(하루 100점)으로는 다섯 달 반이 걸려서, 포인트를 올려 **넉 달**쯤에 맞췄다 (위 "하루에 받을 수 있는 양")
  과제에 하루 다섯 번 한도가 붙어서 과제 몫은 많아야 하루 100점이다.
  그래도 너무 빨리 다 사지면 `TASK_CAP` 부터 다시 볼 것
- **상점 칸은 비싼 것부터 늘어놓는다** (`byPrice`). 값이 같으면 칸 폴더 `index.ts` 에 적은 순서, 기본 벽 · 바닥(0점) 은 맨 뒤.
  시안실도 같은 함수를 쓴다. 새 아이템을 어디에 적든 칸 안 순서는 값이 정한다
- **살 때는 반드시 한 번 묻는다.** 벗을 수는 있어도 **환불은 없다.** 제일 비싼 게 1,000점이라
  잘못 눌러 날리면 아프다. 모자라서 못 살 때도 팝업으로 알려준다 —
  상점 아래 한 줄로 흘리면 못 보고 계속 누른다
- **먹이·배고픔은 안 넣는다.** 며칠 못 열면 미안해서 앱을 아예 안 열게 된다.
  판다를 누르면 하트가 뜨는 정도의 공짜 귀여움만

### 상점 파일 — 아이템 하나 = 파일 하나

9/17 에 `lib/pet.ts` 한 파일(1,345줄) 에 몰려 있던 걸 `src/shop/` 으로 나눴다.
가구 · 인형 **방향 돌리기**를 넣으면 아이템마다 앞 · 옆 · 뒤 그림이 붙어서 한 파일로는 못 버틴다.

```
src/shop/
  index.ts        ITEMS · WALLS · FLOORS · itemById … 밖에서는 "@/shop" 하나만 가져다 쓴다
  types.ts        Item · ItemDef(칸 없는 Item) · Surface
  rules.ts        등급 · spriteAt · isWorn · byPrice · CAT_ROWS
  list.ts         inCat — 폴더 목록이 칸을 붙인다
  common/         두 아이템 이상이 같이 쓰는 것만
    palette.ts    INK · 기본 색표 P · s() · FURN · GOLD
    draw.ts       outline · flipRows
    window.ts     창문 틀 · win() · 해 · 구름 · 나무
    bed.ts        침대 그림 (침대 · 캐노피 침대)
    bow.ts        왕리본 (분홍 · 하늘)
  clothes/ furniture/ doll/ prop/ wall-deco/
    <id>.ts       아이템 하나. **파일 이름 = 아이템 id**
    index.ts      그 칸의 목록
  wallpaper.ts · floor.ts   벽지 · 바닥 목록
```

- **새 아이템** — 칸 폴더에 `<id>.ts` (`ItemDef` 를 default 로) + 그 폴더 `index.ts` 에 한 줄. **목록에 안 넣으면 상점에 안 뜬다**
- **칸(`cat`) 은 아이템 파일에 안 적는다.** 폴더 목록이 `inCat` 으로 붙인다. 폴더를 옮기면 칸이 같이 바뀐다 —
  두 군데 적으면 한쪽만 고쳐서 어긋난다 (등급을 값 하나로 정한 것과 같은 이유)
- **`common/` 에는 둘 이상이 쓰는 것만.** 한 아이템만 쓰는 부품(눈송이 자리 · 달 · 물고기) 은 그 아이템 파일에 둔다.
  창문 일곱 종도 파일은 따로지만 틀은 `common/window.ts` 하나라, **틀을 고치면 창문이 다 바뀐다**
- **벽지 · 바닥은 파일 하나씩으로 안 나눴다.** 그림이 아니라 무늬 종류 + 색이고, 같은 무늬(`check`) 를 여럿이 쓴다.
  실제 무늬는 여전히 `PetRoom` `backdrop` · `PetView` `swatch` 두 군데에서 그린다 (옮기려면 무늬 하나 = 파일 하나가 맞다)
- **id 는 바꾸지 않는다.** Firestore 의 `owned` · `spots` · `worn` 이 id 로 찾는다
- `lib/pet.ts` 에는 방 크기(`ROOM` · `floorTopAt` …) · `spotsFromPlaced` · 포인트 상수만 남았다
- 나누면서 그림이 안 바뀐 건 **옮기기 전후 `ITEMS` · `WALLS` · `FLOORS` 를 JSON 으로 떠서 비교**해 확인했다 (39개 · 칸 안 순서까지 같음)

### 시안실 — 시안 페이지는 딱 하나 (꼭 지킬 것)

**https://claude.ai/artifact/UDNorSgjhVJt2oyJUbdpQu** — 키우기 전용 시안 페이지.
**새 시안 페이지를 만들지 말고 늘 이 주소 하나를 고쳐 올린다** (사용자 요청, 9/17).
시안마다 페이지를 새로 만들었더니 링크가 쌓이고 지난 것은 지워져서 열리지도 않았다.

- 상점의 **모든 아이템 · 벽지 · 바닥**과 **아직 안 넣은 시안**을 눌러서 방에 놓아볼 수 있다.
  처음 열면 **빈 방**이다. 옮기기 · 방 비우기 · "시안만 보기" · "안 산 모양으로 보기"(프리미엄 잠김 칸 확인용) 가 있다
- **앱 코드를 그대로 묶는다** — `PetRoom` · `PetView` 의 `Good` · `swatch` · `withItem` · `shop/`.
  그래서 앱에서 그림을 고치면 다시 만들기만 하면 시안실도 똑같아진다. 위 셋을 export 한 건 이것 때문이다
- **폰 화면으로 보기** (9/21) — 스위치를 켜면 앱 껍데기(`.device`) 안에 **진짜 `PandaView`** 가 들어간다.
  조작줄 자리 · "한 화면에 들어가나" 를 폰에서 바로 본다. 방은 시안실과 같은 `pet` 을 쓰고, 포인트만 크게 줘서 상점을 다 열어본다.
  이것 때문에 **CSS 를 앱과 똑같이 Tailwind 로 태운다** — 전에는 `@tailwind` 줄만 빼고 썼는데 그러면 유틸이 빠져 스티커 격자가 무너진다
- 파일은 `tools/pet-preview/`
  - `drafts.ts` — **아직 상점에 안 넣은 시안.** 새 시안은 여기에 넣고, 채택되면 `src/shop/<칸>/<id>.ts` 로 옮기며 여기서 지운다.
    떨어진 것도 지우고 이유는 이 파일 "아직 안 한 것" 에 남긴다
  - `App.tsx` · `preview.css` — 페이지. CSS 는 앱 `globals.css` 를 그대로 쓰고 이것만 덧붙인다
  - `build.mjs` — `node tools/pet-preview/build.mjs` → `tools/pet-preview/out/index.html` (out 은 git 에서 빠진다)
- **올리기**: Artifact 도구로 `out/index.html` 을 **`url` 에 위 주소를 넣어** 올린다. `url` 없이 올리면 새 페이지가 생긴다
- 넘어진 것 — esbuild `--define` 에 `"production"` 을 넘기면 윈도 셸이 따옴표를 먹어서 이름이 돼버렸다.
  `env-shim.js` 를 `--inject` 로 넣는다. 앱 글꼴(DungGeunMo) 은 시안실에서 막혀서 `@font-face` 를 빼고 만든다.
  **그 빼는 정규식이 `\s` 대신 `s` 로 적혀 있어서 한동안 안 빠졌다** (9/17 고침) — 올릴 때마다 "글꼴을 다른 사이트에서 불러온다" 경고가 떴다
- 넘어진 것 — 대화를 새로 열면(`/clear` 포함) 첫 올리기가 **"live version 을 안 봤다" 며 거절된다.**
  거절될 때 지금 올라가 있는 페이지를 받아주므로, 손으로 고친 게 없는지 보고 다시 올리면 된다. 시안실은 빌드 결과라 그대로 다시 올려도 된다

### 상점 등급 — 값으로 정한다

| 등급 | 값 | 상점 칸 | 지금 있는 것 |
|---|---|---|---|
| 일반 | ~ 499 | 보통 칸 (안 샀으면 회색 · 자물쇠) | 42개 (조선 세트 소반 450 · 달항아리 420 · 비단 방석 380 포함) |
| **골드** | 500 ~ 999 | 금테 두 겹 · 크림빛 금 바탕 · 왼쪽 위 **왕관** · 3초마다 빛이 스친다 | 금실 줄무늬 벽지 700 · 꽃 타일 650 · 왕관 600 · 요정 날개 650 · 캐노피 침대 750 · 공주 화장대 800 · 어항 650 · 밤하늘 창문 550 · 왕실 깃발 550 · 왕좌 850 · 유니콘 인형 600 · 장미 꽃병 500 · **조선 세트 10** (창호문 · 병풍 700 · 자개장 690 · 먹빛 벽지 680 · 화관 · 반닫이 650 · 대청마루 640 · 민화 · 갓 620 · 청사초롱 600) |
| **프리미엄** | 1,000 ~ | **보라 바탕** · 금테 · 왼쪽 위 **보석** · 반짝이는 별 셋 · 금빛이 스친다 | 궁전 창문 · 발코니 창문 · 궁전 벽지 · 왕실 카펫 · 왕실 침대 · **먹빛 전돌 바닥** |

- **`gradeOf(price)` 하나로 정한다** (9/17 사용자가 구간을 정했다). 경계는 `GOLD_FROM` · `PREMIUM_FROM`.
  전에는 아이템마다 `premium` · `legend` 표를 달았는데, **값을 바꿀 때 표를 같이 안 고치면 어긋나서** 없앴다.
  이제 값만 바꾸면 칸 모양이 따라온다
- 이름이 한 번 바뀌었다 — 금테가 "프리미엄", 보라가 "전설" 이던 것을 **금테 = 골드 · 보라 = 프리미엄** 으로.
  CSS 도 `.good-gold` · `.good-premium` 으로 바꿨다. 옛 이름이 보이면 이 전의 것이다
- **안 산 칸도 회색으로 죽이지 않는다** — 골드는 금테와 바탕, 프리미엄은 보라빛을 그대로 두고 그림만 흐리게.
  일반 칸처럼 회색이 되면 비싼 게 눈에 안 띄어서 모을 마음이 안 생긴다
- 프리미엄 보라는 사용자가 "겉 배경이 보라색이면서 귀한 느낌" 을 원했다. 자물쇠도 밝은 색으로 바꾼다 (보라 위에서 안 보여서).
  스치는 빛은 골드 규칙을 같이 쓰고 색만 금빛으로 덮어써서, **`.good-premium` 블록이 골드 뒤에 있어야 한다**
- **움직이는 것(`anim`)** — 어항 8장 · 밤하늘 창문 4장(별 두 벌을 두 장씩) · 요정 날개 2장.
  `PetRoom` 이 `ANIM_MS`(0.4초) 마다 `tick` 을 올리고 `spriteAt(it, tick)` 으로 장을 고른다.
  - 판다 걸음과 **따로** 돈다. 옮기기 중엔 판다가 멈추는데 거기에 묶으면 어항도 멈춘다
  - 움직이는 게 방에 없으면 타이머를 안 돌린다. 줄이기 설정(reduced motion) 이면 첫 장에 멈춘다
  - `spriteAt` 은 **장마다 같은 객체**를 돌려준다. 매번 새로 만들면 `Piece` memo 가 안 먹어 0.4초마다 방 전체를 다시 그린다
  - 상점 칸에는 첫 장만 보인다
- **요정 날개는 `back` 자리** — 판다 몸보다 **먼저** 그려야 뒤로 간다. `head` · `body` 와 따로 걸치므로
  멜빵바지와 같이 입을 수 있다. `worn.back` 이 Firestore 에 새로 생긴다
- 캐노피 침대는 침대 그림(`shop/common/bed.ts`) 위에 지붕 · 커튼을 올린 것이라 **침대를 고치면 캐노피도 바뀐다**
- 벽지 · 바닥 무늬(`kind`) 는 **`PetRoom` 의 `backdrop` 과 상점 미리보기(`swatch`) 두 군데**에 그린다. 새 무늬는 둘 다 넣을 것.
  궁전 벽지(`palace`) · 왕실 카펫(`royal`) · 꽃 타일(`flowertile`) · 금실 줄무늬(`goldstripe`)
- **궁전 벽지는 왕실 카펫과 한 벌**이다 — 카펫의 크림 · 진홍(`#C8384F`) · 금을 그대로 쓴다. 카펫 색을 바꾸면 벽지도 같이 볼 것.
  처음엔 분홍 바탕 · 분홍 마름모였는데 카펫과 따로 놀아서 9/17 에 바꿨다
- **대리석 바닥(`f7`, 240) 은 9/17 에 뺐다.** 깔아둔 사람은 `floorById` 가 못 찾아 맨바닥으로 보인다. 환불은 없다

### 포인트를 다루는 규칙 (어기면 조용히 어긋난다)

- `earned` 를 바꾸는 곳은 **다섯**이다 — 스티커 토글, 과제 토글, 과제 삭제,
  토익 퀴즈 만점, 타이머 25분. 더 늘리지 말 것. 늘어날수록 어긋날 자리가 는다
- **하루에 몇 번까지만 주는 것은 준 날짜를 같이 적는다** — `quizDay` ·
  `focusDay`/`focusCount` · `taskDay`/`taskCount`. 판다 문서에 두는 건 포인트를 건드리는 곳과
  세는 곳이 갈리면 어긋나기 때문이다. 날짜는 화면에서 받아 넣는다 —
  `useRewards` 안에서 `new Date()` 를 부르면 자정을 넘겨도 어제에 머문다
- 스티커 문서와 `earned` 는 `writeBatch` 로 **같이** 쓴다. 따로 쓰면 하나만 성공했을 때 어긋난다
- **과제는 배치가 아니라 트랜잭션이다.** 과제 문서를 다시 읽어 **이미 그 상태면 아무것도 안 한다.**
  배치로 화면의 `done` 을 믿고 `increment(±20)` 만 보내던 때는, 화면이 갱신되기 전에
  두 번 누르면 20점이 두 번 붙었다. 대신 **오프라인에서는 체크가 안 된다** (트랜잭션은 서버가 필요하다)
- **과제에는 실제로 준 점수(`paid`) 와 그 날(`paidDay`) 을 적는다.** 풀거나 지울 때 이만큼만 뺀다.
  한도를 넘겨 0점으로 체크된 것을 풀면서 20점을 뺏지 않으려는 것이다.
  `paid` 가 없는 건 이 규칙 전에 체크한 것이라 20점을 줬다고 본다
- **같은 날 체크하고 풀면 한도도 한 칸 돌려준다.** 어제 체크한 걸 오늘 풀면 점수만 빠진다
- **과제를 고칠 때(`save`) 는 `done` 을 안 쓴다.** 화면의 옛날 `done` 으로 덮으면
  점수를 안 돌려받고 체크가 풀려서 다시 받을 수 있다. `done` 은 `toggle` 만 바꾼다
- 스티커 점수는 그 달 개수로 다시 계산한 **차이**(`stickerPoints(after) - stickerPoints(before)`).
  떼면 음수라 10개째·연속 보너스도 반납된다. **오늘 칸만** 누를 수 있고, 연타하면 붙였다 뗀 것이라
  합이 0 이다. 그래서 스티커는 한도를 따로 안 뒀다
- **다 했다고 표시된 과제를 지우면 준 점수도 회수한다.** 안 그러면 만들고 → 체크하고 →
  지우기를 되풀이해 점수를 불릴 수 있다. 체크 여부는 `remove` 가 트랜잭션 안에서 다시 읽는다
- **잔액은 음수도 그대로 보여준다.** 사고 나서 떼거나 풀면 빚이 남는데, 예전에는 0 으로 가려서
  "풀어도 안 깎이고 다시 붙여도 안 는다" 로 보였다 — 새는 것처럼 보였지만 실제로는 안 샌다
- **`spots` 는 맵이 아니라 배열이다.** `merge` 로 쓰면 맵은 **키가 안 지워져서**
  가구를 치워도 방에 그대로 남는다. 배열은 통째로 갈린다
- `usePet` 의 `write` 는 **`earned` 를 빼고 `merge` 로** 쓴다.
  통째로 덮으면 방금 붙인 점수를 화면이 들고 있던 옛날 값으로 되돌린다

---

## 윷놀이 이벤트 (한시적 · 9/21 넣음)

7일짜리 이벤트다. **정연은 진짜 포인트, 상대는 숫자만** 걸고 한 계정으로 둘이 번갈아 한다.
사용자가 정한 것 — 말 3개 · 잡기 · 업기 · 지름길 · 백도 · **한 판 100점** ·
**이기면 받기만 하고 져도 안 깎인다** · 기간이 끝나면 **손으로 뺀다**(날짜로 저절로 사라지게 하지 않았다).

### 끝나면 지울 것 (이 목록대로만 지우면 된다)

```
src/lib/yut.ts · components/EventView.tsx · YutBoard.tsx · YutThrow.tsx · tools/yut-check.mts
globals.css 의 "윷놀이 이벤트 탭 (한시적)" · "이벤트 잠금 (한시적)" 두 덩어리 통째로
TabBar  — ICONS.event · LABELS 의 ["event","이벤트"] 줄 · LOCK · eventLocked · onLocked
AppRoot — EventView import · useYut · useEventLock · tab === "event" 칸 · scroll 의 ev 클래스
        · 잠금 팝업(locked · "울 아가 혼나요") · globals.css 의 .tab-lock-on · .tab-lock
types.ts — TabKey 의 "event" · YutGame · store.ts 의 useYut · useEventLock · EMPTY_GAME
GuidePopup — PER_WIN import 와 WAYS 의 윷놀이 칸
        · LOCK_PW · LockMark · LockPad · lockbox 칸 · eventLocked/onSetEventLock
tools/pet-preview/App.tsx — TabBar 에 넘기는 onLocked · eventLocked
```

Firestore 문서(`event/yut`) 는 남겨둬도 그만이다. **일부러 다른 곳을 안 건드렸다** —
포인트를 주는 자리 하나만 빼면 기존 코드에 손댄 데가 없다.

### 길 — 밭 번호가 곧 규칙이다

밭 29개에 번호를 매겼다. **0 = 참먹이 자리(오른쪽 아래)** — 거기서 **오른쪽 변을 타고 위로**
올라가 반시계로 돈다(우하 → 우상 → 좌상 → 좌하 → 우하). 지름길은 21~29,
**20 = 골**(참먹이를 지나쳐 판을 떠난 말).

**9/22 에 방향과 규칙을 한꺼번에 바로잡았다** — 사용자가 "우측 하단에서 우측 위로 가야 하지 않느냐" 고
짚어준 데서 시작해 대회규정 · 위키를 찾아보고 셋을 고쳤다.

- **길 방향이 좌우로 뒤집혀 있었다.** 우하에서 아랫변을 따라 좌하로 갔다 — 주석에는 "반시계" 라고
  적혀 있었으니 주석과도 어긋나 있었다. 좌표를 뒤집으니 첫 지름길이 우상 → 방 → 좌하(찌모) 가 된다.
  출발점의 빨간 화살표도 진행 방향(위) 으로 돌렸다
- **참먹이는 밭이다 — 서면 아직 안 난 것이고, 지나야 난다.** 마지막 밭(19) 과 둘째 지름길 끝(29) 이
  골이 아니라 **참먹이(30 · 31)** 로 이어진다. 방에서 걸이면 28 · 29 를 지나 참먹이에 **선다.**
  참먹이에 선 말은 **거기서 잡힌다.** 딱 맞게 나갈 필요는 없다 — 지나치기만 하면 난다
- **'도'(1) 에서 백도면 참먹이에 선 것으로 친다(32).** 다음엔 뭐가 나오든 나고, 백도가 연달아
  나오면 도로 '도' 자리로 간다. 전에는 출발점으로 돌아가 **한 바퀴를 다시 돌아야 했다**
- **백도는 온 길로 되물린다** (대회규정: 물리는 방향은 직전에 움직인 방향의 반대).
  들어오는 길이 둘인 밭은 이름을 나눠 뒀다 — **방 33**(둘째 지름길) · **34**(첫 지름길),
  **찌모 15**(바깥길) · **35**(지름길), **참먹이 30 · 31 · 32**.
  이름만 다르고 자리는 같으므로 **잡기 · 업기 · 수 고르기는 `field()` 로 한 밭으로 모아** 따진다.
  업으면 되물릴 길도 **방금 들어온 말** 을 따라간다(`applyMove`) — 업은 덩어리는 같이 움직이니까
- **23 = 지나가는 방 · 33 · 34 = 멈춘 방.** 멈추는 순간 바꿔두면
  "방에 서면 골로 질러간다" 가 길 표(`NEXT`) 하나로 저절로 풀린다. 분기문이 없다
- **갈림길은 멈춘 말이 떠날 때만 열린다** (`FORK`). 모서리를 지나쳐 가면 바깥길이다.
  9/22 까지는 **늘 지름길을 태웠는데**, 일부러 돌아서 잡으러 가는 수가 아예 없어서
  9/23 에 고르게 했다 (사용자 요청). 갈림길은 셋 — **우상 모서리(5) · 좌상 모서리(10) ·
  첫 지름길로 멈춘 방(34)**. 둘째 지름길로 멈춘 방(33) 은 나머지 팔이 전부 왔던 길 쪽이라 갈림길이 아니다.
  `movesFor` 가 **`from` 이 같고 `to` 가 다른 수 둘**을 내놓고, 화면이 말을 누르면 갈 곳을 묻는다
- **`pathOf` 는 거쳐 가는 밭을 순서대로 준다** — 말이 한 칸씩 걸어가는 데 쓴다.
  `advance` 와 같은 `run()` 하나에서 나오므로 **길이 어긋날 수가 없다**
- **골 번호(20) 는 건드리지 않았다.** 참먹이에 30~32 를 새로 준 것도 이 때문이다 —
  이벤트가 도는 중에 20 의 뜻을 바꾸면 Firestore 에 이미 난 말이 참먹이로 살아난다.
  같은 이유로 **예전 방식으로 0 에 서 있던 말은 참먹이로 읽는다**(`old0`)
- **넘어진 곳 — 번호가 크다고 골이 아니다.** 지름길 밭이 21~29 라 `next >= GOAL(20)` 로 끊었더니
  **지름길에 들어서는 순간 나 버렸다.** 화면을 안 띄우고 `tools/yut-check.mts` 로 잡았다.
  `node --experimental-strip-types tools/yut-check.mts` — 길 · 잡기 · 업기 · 윷가락 판정 59가지를 센다.
  규칙을 건드리면 이것부터 돌릴 것
- 업은 말은 `movesFor` 가 **수 하나로 묶는다.** 같은 밭에 선 내 말은 늘 같이 움직인다
- 백도인데 **판에 말이 하나도 없으면 도로 친다**(`realThrow`). 안 그러면 첫 턴부터 아무것도 못 한다.
  기록(`log`) 에도 바꾼 값을 적는다 — "백도" 라고 적어놓고 도로 가면 속은 것처럼 보인다

### 판 한 번의 흐름

`playing` · `winner` 두 값이 화면을 셋으로 가른다 (9/21 사용자 요청으로 시작 · 끝 화면을 넣었다).

| 상태 | 화면 |
|---|---|
| `playing: false` · `first: null` | 판이 **어둡게 덮이고 가운데 "게임 시작"** 만. 아래 버튼은 아예 없다 |
| `playing: false` · `first` 있음 | **선 뽑기** — 둘이 한 번씩 던져 높은 쪽이 선 |
| `playing: true` | 던지고 옮긴다 |
| `winner` 있음 | **이긴 창**(누구 승 · 포인트 · 전적) → 확인을 누르면 다시 시작 화면 |

**선은 전통대로 뽑는다** (9/21 사용자가 고른 것 — "이긴 쪽이 먼저" 도 후보였다).
시작을 누르면 `first: { a: null, b: null }` 이 열리고, 정연부터 한 번씩 던진다.
**모 > 윷 > 걸 > 개 > 도 > 백도**(`rank()` — 백도는 판에서는 뒤로 가는 값이라 여기서는 맨 아래) 로 견주고,
같으면 둘 다 비워 다시 던진다. 선이 정해지면 그 편으로 `start(game, lead)` 한다.
- 선 뽑기에서는 **윷 · 모가 나와도 한 번 더 던지지 않는다** (`YutThrow` 의 `draw`). 한 번씩만 던져 견주는 자리다
- 뽑은 값도 Firestore 에 들어간다 — 각자 폰에서 봐도 같은 화면이 보여야 해서다

- 판 번호(`round`) 는 **시작할 때** 올린다. `finish` 가 "이 판에 이미 줬나" 를 그 번호로 보기 때문에,
  닫을 때 올리면 같은 번호로 두 번 줄 수 있다
- 지난 판에서 **이긴 쪽이 다음 판을 먼저 던진다.** 창을 닫을 때 `turn` 에 옮겨 담는다
- **판 초기화** (9/23 사용자 요청). 두던 판을 접고 시작 화면으로 돌아간다 —
  잘못 시작했거나 그만두고 싶어도 끝까지 두는 수밖에 없었다.
  **전적(`wins` · `paid`) 과 판 번호는 그대로 둔다** — 번호를 되돌리면 `finish` 의
  `paidRound` 와 어긋나 같은 번호에 두 번 줄 수 있다. 버튼은 판 아래 **점선 글씨 하나**로 조용히 둔다

**던질 빚은 숫자다 (`owe`)** — 9/23 에 고친 버그다. 전에는 `pending` 참/거짓 하나라
**빚이 둘이어도 한 번으로 뭉개졌다**: 윷을 던져 빚 하나(윷·모), 그 윷으로 잡아 빚 둘,
그런데 다음 던지기가 `pending = isExtra(값)` 으로 **덮어써져서** 한 번이 날아갔다.
차례가 오면 1, 던지면 1 줄고, 윷·모나 잡기면 1 는다. `rolls` 가 비고 `owe` 가 0 이면 차례가 넘어간다.
**옛 문서 호환** — `owe` 가 없으면 `store.ts` 의 `readGame` 이 `pending` 을 1/0 으로 읽는다
- 덮개는 `.ev-board-wrap`(position: relative) 안에 있어서 **판만 덮는다.** 점수판과 기록은 계속 보인다
- 이긴 창은 `.dim` 이라 앱 전체를 덮는다 — `.scroll` 에 position 이 없어서 `.device` 를 기준으로 눕는다 (타이머와 같은 수법)

**윷 · 모가 나와도 옮기고 나서 던지는 게 아니다** (9/22 사용자 요청).
던질 기회가 남아 있으면 `한 번 더 던지기` 가 계속 서고 던진 값이 `rolls` 에 쌓인다.
**다 던진 뒤 패를 눌러 쓸 값을 고르고** 말을 옮긴다 — 어느 값부터 쓸지 보고 정할 수 있다.
순서를 앱이 강요하지 않는다: `새 말 내보내기` · `건너뛰기` 는 던지기 버튼과 `.ev-acts` 한 줄에
나란히 서서, 먼저 옮기고 던지는 것도 그대로 된다 (잡아서 생긴 한 번 더도 같은 자리에 붙는다).

**기록(`log`) 은 이번 "차례" 것만 담는다** (9/22 사용자 요청 — 한 폰으로 번갈아 던지니
둘의 기록이 섞여 보기 어려웠다). 차례가 넘어갈 때 `spend` 가 같이 비운다.
머리줄에 **지금 던지는 쪽 이름**을 붙이고, 패 테두리도 그 편 과일 색이다
(정연 딸기 빨강 · 병근 망고 주황 — 점수판의 과일과 짝이 맞아 따로 외울 게 없다).

### 포인트 — `earned` 를 건드리는 여섯 번째 자리

판을 끝내는 `finish` 만 포인트를 만진다. **배치가 아니라 트랜잭션**이다 —
판 문서를 다시 읽어 **그 판(`paidRound`) 에 이미 줬으면 아무것도 안 한다.**
한 계정으로 둘이 하다 보면 끝나는 순간이 두 번 눌릴 수 있다 (과제 체크와 같은 이유).
**정연(b) 이 이겼을 때만** 100점을 준다. 진 판은 아무 일도 안 일어난다.

### 화면

- **앱의 분홍을 안 쓴다.** 먹 · 고동 · 한지 · 단청으로만 칠했다 (사용자 요청 —
  "이벤트성이니 고동색 · 흑석색 같은 한국 느낌으로"). 색은 전부 `globals.css` 의 `.ev` · `.yut` 아래에만 있어서
  다른 탭에 안 번지고, 지울 때도 그 덩어리만 지우면 된다. 헤더와 아래 탭은 앱 그대로 분홍이다
- 맨 위가 **현판**(먹판에 금 글씨) + **색동 띠**, 그 아래 점수판 · 판 · "던진 윷" 함 · 버튼.
  현판에 있던 **기간 · D-day 는 뺐다** (9/23 사용자 — 언제까지인지 말로 하지 않기로 했다).
  `config.ts` 의 `EVENT` 도 그래서 지웠다
- **말이 한 칸씩 걸어간다** (9/23 사용자 요청). 순간이동하면 지름길을 탔는지 돌았는지 안 보인다.
  `pathOf` 가 준 밭을 **0.13초에 하나씩** 밟고, 다 걸은 뒤에야 판 문서를 고쳐 쓴다 —
  걷는 동안 판을 못 누르게 막아야 한 수가 두 번 가지 않는다.
  화면만 앞질러 그리는 것이라 **상대 폰에서는 도착한 자리로 바로 바뀐다** (판 문서는 한 번만 쓴다).
  줄이기 설정이면 걷지 않는다
- **갈림길을 누르면 갈 곳 둘이 금빛 과녁으로 깜빡인다** (9/23). 고를 말은 점선, 갈 곳은 꽉 찬 테두리 —
  둘이 같은 모양이면 무엇을 누르는 건지 헷갈린다. `다시 고르기` 로 무른다
- **밭 이름을 판에 적었다** (9/23 사용자 요청) — 도 · 개 · 걸 · 윷 · 모 · 꺾 · 찌모 · 방 · 참먹이 아홉 개만.
  스무 밭에 다 붙이면(모도 · 모개 · 꺾도 …) 판이 글자로 덮인다.
  **도트가 아니라 진짜 글자다** — 세 글자를 5칸 도트로는 못 쓴다. 뷰박스 78칸에 `font-size: 2.4` 라
  폰에서 11px 남짓(갈무리 원래 크기)이고, 더 키우면 `참먹이` 가 밭을 넘는다.
  **말보다 먼저 그려서 말이 덮는다** — 말이 선 밭은 이름을 볼 일이 없다.
  참먹이에 있던 **빨간 화살표는 뺐다** — 오른쪽 변에 도 → 모 가 올라가니 이름이 곧 방향이고,
  말이 걸어가는 것도 방향을 보여준다
- **점수판에는 이름 · 차례 · 말 셋만 둔다.** 처음엔 "몇 승 · 몇 P · 진짜/가상 포인트" 도 적었는데,
  판마다 100점 고정이라 셀 이유가 없다고 해서 뺐다 (9/21 사용자).
  `wins` · `paid` 는 문서에 그대로 쌓이므로 **되살리려면 `card()` 만 고치면 된다**
- **편 이름은 병근 · 정연** (`NAME`). 한 계정으로 둘이 번갈아 하니 화면에 이름을 그대로 적는다 (9/21 사용자)
- **말은 딸기(정연) 와 망고(상대).** 판다 방과 같은 방식으로 도트를 rect 로 찍는다.
  업으면 위로 겹쳐 쌓고 **먹빛 패에 금색 숫자**를 붙인다
- **점수판의 말 셋은 세 모양이다** — 안 나간 말 `빈 칸` · 판 위의 말 `과일` · 난 말 `먹빛 패에 금색 체크`.
  9/23 까지 난 말은 **과일의 윤곽만** 남겼는데 빈 칸과 구분이 안 됐다 (사용자 지적).
  체크는 업은 말 숫자패(`count`) 와 같은 먹 · 금이라 "다 됐다" 로 읽힌다.
  **안 나간 말 표기는 9/21 것 그대로 뒀다** — 헷갈린 건 난 말 쪽이었다
- 판 바탕은 한 번 만들어 두고(`BACKDROP`) 말만 다시 그린다
- **윷 던지기는 판 위 모달.** 멍석 위에서 가락 넷이 솟았다 떨어지고 튄다 —
  키프레임이 아니라 매 틱 속도 · 중력을 셈한다. **자리는 정수 px 로만** 옮긴다(도트가 어긋난다).
  도트 그림이라 회전시키지 않고 **면 네 장을 갈아끼운다.** 그림자가 높이 따라 줄어든다.
  **좌우로도 흩어진다** (9/23) — 가운데 둘은 조금, 바깥 둘은 바깥쪽으로 더 갈라지고,
  멍석 끝(`SPREAD` 9px) 에 닿으면 되튄다. 바닥에 닿을 때마다 옆으로 가는 힘이 반씩 죽는다.
  그림자도 같이 옮긴다
  줄이기 설정이면 굴리지 않고 결과만 보여준다
- **넷째 가락이 백도 가락**이다. 그림에도 단청 적색 점을 새겨서 보면 안다

### 잠금 — 가이드에서 몰래 여닫는다 (9/22)

9/21 에는 **`TabBar` 의 `LOCKED` 배열**이 잠금이었다. 여닫을 때마다 코드를 고쳐 올려야 해서
**앱 안에서 여닫도록** 옮겼다 (사용자 요청). 자물쇠가 붙는 모양과
"울 아가 혼나요" 팝업은 9/21 것 그대로다 — **바뀐 건 무엇이 잠금을 정하느냐뿐이다.**

**열쇠는 가이드 → 업데이트 내역 맨 아래**의 색 없는 점선 칸이다. 목록에서 한 칸 떨어뜨리고
글씨 10px · 회색뿐이라 눈이 그냥 지나간다. **색을 안 쓰는 게 숨기는 방법이다.**

- **잠금은 바로 잠근다. 푸는 것만 네 자리(`2320`)를 받는다** (사용자가 정했다).
  실수로 잠그는 건 다시 풀면 그만이지만, 실수로 열리면 들킨다
- 비밀번호 창은 **숫자판을 직접 그린다**(`LockPad`). 폰 자판이 올라오면 창을 덮는다.
  네 자리가 차는 순간 판정하고, 틀리면 비우고 흔든다
- 가이드 **안**이 아니라 **옆**에 띄운다 — `Popup` 안에 `.dim` 을 또 두면 좁은 칸에 갇힌다

**어디에 저장하나 — `users/{uid}/event/lock` 문서 하나 (`{ locked: boolean }`).**
계정에 두는 것이라 **한 쪽에서 잠그면 다른 쪽 폰에서도 잠긴다** (9/22 사용자 —
계정은 하나이고 둘이 같이 쓴다). **판 문서(`event/yut`) 에 얹지 않았다** —
`useYut` 의 `write` 가 판을 `setDoc` 으로 통째로 덮어써서 같은 문서에 두면
판을 새로 열 때마다 잠금이 날아간다.

- **문서가 없으면 잠긴 것으로 본다.** 불러오는 동안(`null`) 도 잠긴 쪽을 보여준다 —
  늦게 열리는 건 괜찮아도 **잠깐 열려 보이면 안 된다** (`eventLocked = lockRaw !== false`)
- **업데이트 내역에는 아무것도 안 적었다.** 정연 쪽에서 보이는 건 9/21 과 똑같고
  (자물쇠 · "울 아가 혼나요"), 여닫는 자리를 적으면 숨긴 뜻이 없어진다. 그 목록을 정연이 본다

### 시안 — 이 주소 하나만 고친다 (판다 시안실과 같은 규칙)

**https://claude.ai/artifact/QGYYx8jnLVPV8iMnooX1jJ** — 윷놀이 화면 시안.
판다 시안실과 **다른 페이지**다(주제가 다르다). 새로 만들지 말고 늘 이 주소를 고쳐 올린다.

- **앱 글꼴(갈무리) 을 넣는 법을 찾았다** — 아티팩트는 구글 폰트만 불러올 수 있어서 판다 시안실은 글꼴을 빼고 만들었는데,
  `cdn.jsdelivr.net/npm/galmuri/dist/Galmuri11.woff2` 를 받아 **base64 로 박아 넣으면** 된다 (둘 합쳐 0.7MB, 한도는 16MB).
  판다 시안실에도 쓸 수 있다
- 소스는 시안용으로 따로 쓴 HTML 한 장이다 (앱 코드를 묶는 판다 시안실과 다르다).
  **앱에 넣은 뒤로는 앱 코드가 정본**이다 — 시안을 또 고칠 일이 있으면 앱에서 옮겨 심는다
- 9/22 에 말길 방향 · 참먹이 규칙 · 기록 · 던지기 개편을 앱에서 그대로 옮겨 심었다.
  편 이름도 그때 "오빠 → 병근" 으로 맞췄다 (시안에만 옛 이름이 남아 있었다)

---

## 타이머

**시간표 탭 안에 있다.** 아래 탭을 여섯 칸으로 늘렸더니 번잡해서 판다처럼 안에 나눠 넣었다.
서브 탭을 왔다 갔다 하면 화면이 언마운트되지만, 시간을 localStorage 에 들고 있어서 안 끊긴다.

**켜면 앱 껍데기를 통째로 덮는다.** 날짜줄도 아래 메뉴도 안 보이고 화면 전체가 검정,
분홍 시계만 남는다 — 공부하는 동안 볼 화면이라 다른 게 눈에 들어오면 안 된다.
`.timer-full` 은 `.scroll` 안에 있지만 부모에 position 이 없어서 `.device` 를 기준으로 눕는다.
`.dim` 과 같은 수법이라 `.scroll` 의 overflow 에도 안 잘린다.
덮는 동안은 탭을 못 누르므로 **나가는 길은 `돌아가기` 버튼 하나뿐이다** (시간표로 돌아간다).

**버튼은 둘뿐 — `시작하기`/`일시정지` 와 `돌아가기`.** `처음으로`(0 으로 되돌리기) 는 뺐다.
어차피 자정에 지워지고, 공부 중에 잘못 눌러서 잰 시간이 날아가는 게 더 아깝다.

공부 시간을 재는 스톱워치. **기록은 남기지 않는다** — 오늘 잰 시간만 들고 있다가
날짜가 바뀌면 0 으로 돌아간다. 그래서 정한 것들:

- **Firestore 가 아니라 localStorage.** 남길 값이 아니고, 초 단위로 쓰면 쓰기 횟수만 먹는다.
  이 앱에서 유일하게 Firestore 밖에 있는 상태다
- **흐른 시간을 저장하지 않는다.** `acc`(멈춰서 쌓인 시간) 와 `since`(돌기 시작한 시각) 만 두고
  매번 시계로 다시 잰다. 앱을 닫아두거나 화면이 꺼져 있어도 시간이 밀리지 않는다.
  **시작해놓고 앱을 닫으면 그동안도 계속 흘러간다** — 재는 건 공부 시간이지 앱을 켠 시간이 아니다
- **자정 초기화는 `useToday` 가 해준다.** 훅이 오늘 날짜를 받아 쓰기 때문에 날이 바뀌면
  훅이 다시 돌고, 저장된 날짜가 다르면 그때 버린다. 앱을 켜둔 채 자정을 넘겨도 맞는다
- **포인트를 준다 — 조건이 붙는다.** 처음엔 "켜두기만 해도 쌓인다" 며 안 줬는데,
  정작 제일 애쓴 것에 값이 없으니 포인트가 "체크박스 누르는 값" 이 돼버렸다.
  **일시정지 없이 25분** · **하루 네 번**까지 — 이 둘이 켜두기 게임을 막는 전부다.
  일시정지하면 판이 끝나서 `paid` 가 0 으로 돌아간다 (25분을 이어서 채워야 한다는 뜻)
- **점수가 붙으면 화면에 띄운다.** 조용히 주면 받은 줄을 모른다. 띄우는 건
  **실제로 준 횟수를 돌려받았을 때만** 이다 — 네 번을 다 채운 뒤에는 안 뜬다

### 가로로 쓰는 화면

**방향을 미디어쿼리로 묻지 않는다.** 회전 잠금이 켜져 있으면 폰을 눕혀도 브라우저는 계속 세로라
`(orientation: landscape)` 가 영영 안 맞는다. 대신 `ResizeObserver` 로 자리 크기를 재서
**세로로 길면 시계를 직접 90도 돌린다.** 잠금이 꺼져 있으면 눕히는 순간 자리가 가로로 길어져서
안 돌린 채로 그대로 맞는다. 어느 쪽이든 폰을 눕히면 똑바로 읽힌다.

돌리는 방향은 `rotate(90deg)`(시계방향) 이라 **폰은 반대로, 왼쪽으로 눕혀야** 한다.
안내 문구는 없다 — 검은 화면에 시계만 두려고 뺐다.

### 숫자 (`ClockFace.tsx`)

**이 앱에서 유일하게 도트가 아닌 그림이다.** 처음엔 `sprites.ts` 에 도트로 찍었는데,
크게 키우니 획 끝 계단이 그대로 커져서 화면이 정신없었다. 지금은 **획 하나가 육각형 하나**다.
끝이 45도로 매끈하게 깎이고 획끼리 0.9칸 떨어진다. 한 글자 12 x 22, 획 두께 3.

- **꺼진 획은 안 그린다.** 진짜 LCD 처럼 뒤에 8 을 깔아봤지만 그것도 정신없었다
- **도트 느낌은 버튼에만** 남겼다. 각진 테두리 + 도트 폰트. 앱의 `.btn` 과 달리
  그림자(입체) 는 안 쓴다 — 검은 화면에서는 그 변들이 지저분해 보인다
- 시·분·초 전부 두 자리라 **폭이 안 변한다.** 숫자가 바뀔 때 시계가 안 들썩이는 건 이 덕이다

### 자리 잡기

시계는 화면 한가운데, **버튼은 시계 아랫변과 화면 바닥의 딱 중간**에 선다.
`keysY = panelH * 0.75 + 시계높이 / 4` 가 그 자리다 (시계가 가운데 있다는 전제에서 나온 식).
시계 크기를 잡을 때 `BELOW` 만큼 먼저 떼어놓는 건 이 자리를 확보하려는 것이다.

가로로 누우면 화면 높이가 393px 뿐이라 시계는 높이보다 **폭(92%) 에 먼저 걸린다.**
시·분·초 비율이 4.5:1 이라 이보다 크게는 못 키운다.

---

## 가이드 · 업데이트 내역

헤더 왼쪽 위 작은 `가이드` 버튼 → 팝업. 위 칸으로 **포인트 얻기 / 업데이트 내역**을 오간다.

- **포인트 얻기의 숫자는 `pet.ts` 상수를 그대로 읽는다.** 값을 바꿔도 가이드는 안 고쳐도 된다.
  다만 **얻는 방법이 늘거나 조건 글이 달라지면** `GuidePopup.tsx` 의 `WAYS` 를 고칠 것
- **업데이트 내역은 날짜 하나가 옅은 바탕의 큰 칸 하나.** 처음엔 날짜를 테두리 굵은 칩으로,
  줄마다 `새로/바뀜/고침` 꼬리표를 달았더니 버튼이 잔뜩 늘어선 것처럼 지저분하다고 했다.
  지금은 칸 안에서 **종류별로 묶어 소제목(새로 생겼어요 · 바뀌었어요 · 고쳤어요) 을 한 번만** 달고,
  줄 앞에는 종류 색 점만 둔다. 최신 날짜 칸만 분홍빛을 조금 더 준다
- 팝업은 헤더가 아니라 `.device` 바로 아래에 그린다. 헤더는 `position: relative` 라
  그 안에 두면 `.dim` 이 헤더 크기에 갇힌다

### 업데이트 내역 적기 (작업마다 꼭)

작업 일지(아래) 는 개발자용, **업데이트 내역(`src/lib/updates.ts`) 은 앱 쓰는 사람용**이다.
앱에서 보이거나 느껴지는 게 바뀐 작업이면 둘 다 적는다.

- **같은 날짜는 한 칸.** 오늘 날짜 칸이 이미 있으면 그 `items` 에 더하고, 없으면 **맨 위에** 새 칸을 만든다
- 종류는 셋 — `new`(새로) · `change`(바뀜) · `fix`(고침)
- **개발 말 금지.** 트랜잭션·Firestore·컴포넌트·상수·버그 같은 말 대신
  "무엇을 할 수 있게 됐는지 / 뭐가 달라 보이는지" 로 쓴다. 한 줄, 짧게, 존댓말(~요)
  - ✗ `과제 toggle 을 runTransaction 으로 바꿔 중복 increment 방지`
  - ○ `체크를 빠르게 두 번 누르면 포인트가 두 번 들어오던 문제`
- **딱 알면 되는 것만.** 아이템 이름 · 값 · 색 · 모양 설명을 늘어놓지 않는다 (9/17 사용자가 "어렵게 적혀 있다" 고 했다)
  - ✗ `👑 비싼 아이템 8개 — 왕관 · 요정 날개 · 캐노피 침대 · …`
  - ○ `신규 아이템 17종 추가` · `아이템 2종 삭제` · `일부 아이템의 가격 · 모양이 바뀌었어요`
  - 포인트 숫자 · 등급 구간 같은 자세한 값도 빼고 "받는 포인트가 늘었어요" 정도로. 자세한 건 가이드 · 상점에서 보인다
  - 같은 날 비슷한 줄(가격 바뀜 · 모양 바뀜 · 뺀 것) 은 한 줄로 합친다
- 어디 있는지 모를 만한 건 위치를 같이 쓴다 ("시간표 탭 안에 있어요")
- 코드 정리·문서만 고친 작업처럼 **쓰는 사람이 못 느끼는 건 안 적는다**
- 9/11 ~ 9/16 칸은 커밋 기록과 이 파일을 보고 나중에 채운 것이다

---

## 넘어졌던 곳

다시 만나면 시간 버리는 것들. 같은 실수를 반복하지 않으려고 남긴다.

**아이폰에서 끌기는 `pointerup` 이 안 올 수 있다**
끄는 도중 위아래로 움직이면 사파리가 스크롤로 보고 `pointercancel` 을 보낸다. 저장을 `pointerup` 에만
걸어뒀더니 **화면에는 옮겨진 채(임시 자리) 저장은 안 된** 상태가 남았고, 다음에 다른 가구를 잡는 순간
앞의 가구가 원래 자리로 튀었다. 컴퓨터 마우스로는 절대 안 생겨서 한참 몰랐다.
→ `pointercancel` 에서도 똑같이 저장하고, 옮기기를 끝내면 임시 자리를 비운다.
**`touch-action` 은 SVG 안쪽 도형에 걸면 사파리가 무시한다** — 가장 바깥 `svg` 에 건다 (`.room-editing`)

**`steps()` 로 1px 을 나누면 반 픽셀이 나온다**
`steps(2)` 로 `0 → -1px` 을 나누면 중간값이 `-0.5px`. 도트가 반 칸 어긋나서,
뒤에 창문처럼 촘촘한 무늬가 있으면 깨져 보인다. 키프레임에 **정수 구간만** 두고
`steps(1)` 로 보간을 없앤다. 판다 걸음·들썩임·하트 전부 이 방식.

**`querySelector("svg")` 가 엉뚱한 걸 집는다**
방을 다시 그릴 때 이전 그림을 지우는 코드가 상점 버튼 안의 아이콘을 먼저 집어서 지웠다.
방 그림에만 `class="room"` 을 붙이고 그것만 집는다. CSS 도 마찬가지로
`.pet-stage svg` 가 아니라 `.pet-stage > .pet-room > svg.room` 이어야 한다.

**"입는 것" 을 id 목록으로 들고 있지 말 것**
리본 id 를 바꿨더니 목록에서 빠져서, 판다가 걸어가는데 리본만 제자리에 남았다.
자리(slot) 로 판단하면 이름을 바꿔도 안 깨진다 &mdash; 앱은 `slot === "head" || "body"` 로 본다.

**문자열 치환으로 코드를 고칠 때는 먹었는지 확인할 것**
`String.replace` 는 못 찾아도 조용히 넘어간다. 창문을 그리는 코드가 이렇게
통째로 안 들어갔는데 스크립트가 "완료" 를 찍어서 한참 못 찾았다.

**SVG 에는 z-index 가 없다. 그리는 순서가 곧 순서다**
판다와 입은 것은 **항상 마지막에** 그린다. 새 소품을 그 아래에 끼워 넣으면 판다가 가려진다.

**`public/` 은 프로젝트 루트여야 한다**
`src/public/` 에 두면 Next 가 서빙하지 않는다. 지도 이미지가 여기서 한 번 걸렸다.

**Next 16 에서 `next lint` 가 없어졌다**
`npm run lint` 를 돌리면 `lint` 를 디렉토리 이름으로 읽는다. ESLint 설정도 아직 없다.

**작업 트리가 원격보다 뒤처져 있을 수 있다**
9/14 에 로컬이 7커밋 뒤처진 채로 작업하고 있었다. **손대기 전에 `git fetch` 로 확인**할 것.

---

## 아직 안 한 것

### 조선 세트 (9/17 상점에 넣음)

왕궁 세트와 결이 다른 두 번째 세트. 시안실에서 세 번 고쳐(등급 · 갓 · 바닥) 상점에 넣었다. `drafts.ts` 는 다시 비었다.
파일 — 벽 장식 `win_moon` · `cheongsa` · `minhwa`, 가구 `irworobongdo` · `jagae` · `bandaji` · `soban`, 옷 `hwagwan` · `gat`,
소품 `bangseok` · `moonjar`, 벽지 `w_dancheong`, 바닥 `f_maru`. 창호문 · 병풍 · 자개장이 같이 쓰는 점 찍기 도구는 `common/draw.ts` (`blank` · `put` · `gridRows`)

- **9/21 에 먹빛 전돌 바닥(1,000 · 프리미엄) 이 들어와 세트에 프리미엄이 하나 생겼다.** 아래는 9/17 에 정한 것 —
- **전부 골드(600~700)** — 처음엔 프리미엄 5 · 골드 8 로 그렸는데, 사용자가 "왕궁은 프리미엄 같지만 이 세트는 그보다 값어치 있어 보이진 않는다" 고 했다.
  크고 움직이는 것일수록 700 쪽 — 달밤 창호문 · 일월오봉도 병풍 700 · 자개장 690 · 먹빛 한지 벽지 680 · 화관 · 반닫이 650 · 대청마루 640 · 까치호랑이 민화 · 갓 620 · 청사초롱 600
- **작은 소품은 일반** (사용자가 크기를 보고 정하라고 했다) — 소반 다과상 450 (16 x 9) · 달항아리 420 (10 x 10) · 비단 방석 380 (깔개).
  장미 꽃병(10 x 12) 은 500 골드지만, 조선 골드를 600 부터로 올려서 작은 것은 그 밑 등급으로 뺐다
- **족두리 → 갓** (사용자). 챙 20폭(머리 12) · 높은 대우 · 갓끈 구슬이 볼까지. 갓끈을 두 칸만 늘어뜨렸더니 귀 옆에 점처럼 떠서 안 보였다
- **한지 벽지는 새 무늬라 `PetRoom` backdrop · `PetView` swatch 에 같이 넣는다.**
  무늬는 맨 위 띠 · 아래 나무 벽널만, 가운데는 한지 결 몇 점. 벽널을 바탕색을 어둡게 해서 만들었더니 회색이 돼서 나무색을 따로 뒀다.
  **띠는 9/21 에 단청 → 먹빛으로 갈았다** (아래 "먹빛 한지 벽지" 참고). 무늬 이름도 `dancheong` → `meok`
- **바닥은 노란 장판 → 대청마루(우물마루)** (사용자가 "바닥만 유독 안 어울린다" 고 했다). 장판은 격자(`grid`) 를 색만 바꾼 것이라
  큰 타일처럼 보였고 노랑이 튀었다. 새 무늬 `maru` — 긴 귀틀이 앞뒤로 달리고 널 이음이 **칸마다 반 줄씩 엇갈린다**
  (한 줄로 이어지면 다시 격자 타일이 된다). 색은 벽널 · 반닫이와 같은 나무 계열이되 가구보다 밝게. `PetRoom` · `PetView` 에 넣었다
- **가구 넷도 돌린다** (9/17) — 병풍 옆은 접힌 세 폭이 좁은 띠(7폭), 뒤는 그림 없는 크림 배접지. 자개장 옆은 자개 꽃 하나 · 뒤는 검은 뒤판.
  반닫이 옆은 놋쇠 모서리만 · 뒤는 널 세 장. 소반 옆은 10폭, 뒤는 앞을 뒤집어 주전자 · 약과 자리가 바뀐다.
  달항아리 · 방석은 어느 쪽에서 봐도 같아서 안 달았다

### 먹빛 한지 벽지 — 띠만 갈았다 (9/21)

사용자: **"천장쪽 띠가 한국스럽기보다 몽골이나 유목민이 쓸 것 같은 무늬다."**
맞는 말이었다 — 초록 바탕에 흰 · 금 네모가 번갈아 박힌 띠였다.
**벽 · 바닥은 그대로 두고 맨 위 띠만** 갈아 끼우는 시안 둘을 내서 먹빛이 뽑혔다.

- **id 는 `w_dancheong` 그대로다.** 9/17 에 상점으로 나간 뒤라 산 사람 문서에 그 id 가 남아 있다 —
  바꾸면 산 것이 사라지고 다시 사야 한다. 이름 · 색 · 무늬만 바꿨다 (값 680 · 골드 그대로)
- **먹빛** — 검은 띠 여섯 줄, 여덟 칸마다 구름 한 덩이, 아래를 금 실선으로 긋는다.
  구름을 **점으로 흩뿌렸더니 먹 위에 앉은 먼지**로 보여서 한 덩이로 뭉쳤다.
  구름은 먹보다 밝아야 하는데 `shade()` 는 어둡게만 할 수 있어서(1 을 넘기면 16진수가 세 자리가 돼 색이 깨진다) 색을 따로 뒀다
- **기와 처마 (떨어짐)** — 다섯 칸이 기와 한 장(골 · 몸통 · 볕), 아래 두 줄을 가운데로 좁혀 둥근 막새.
  네 칸짜리로 그렸을 때는 끝이 안 둥글어 **피아노 건반**처럼 보였다. 이것도 한국집 느낌은 확실했지만 사용자가 먹빛을 골랐다.
  **단청 · 기와 그리는 코드는 지웠다** (사용자 요청) — 되살리려면 이 항목의 그리는 방법을 보면 된다
- 넘어진 곳 — 띠만 갈아 끼우려고 `PetRoom` 의 벽지 갈래를 통째로 바꿔치기했는데,
  그 갈래 **다음에 있던 바닥 그리는 코드까지 같이 지워져** 방 바닥이 하얗게 나왔다.
  `git checkout` 으로 되돌리고 갈래 끝(`const seam =`) 을 경계로 다시 잘랐다.
  **파일을 잘라 붙일 때는 끝 표시를 파일에서 직접 찾아 쓸 것**

### 먹빛 전돌 바닥 — 벽지와 한 벌 (9/21)

먹빛 벽지를 넣고 나니 **조선 세트 바닥이 대청마루(나무) 뿐**이라, 사용자가
"벽지는 귀족 같은데 바닥만 평민 같다" 고 했다. 궁궐 · 절 마당에 깔던 **검은 전돌(方塼)** 로 그렸다.

- 한 칸 건너 아주 조금 밝게 해서 **구운 흙이 장마다 다른 것**처럼. 줄눈은 바닥보다 **어둡게** 판다 —
  밝으면 돌이 아니라 타일 스티커로 보인다
- 칸 한가운데 **작은 마름모**를 찍는다. 처음엔 가로 두 칸짜리 점이었는데 **긁힌 자국**으로 보였다.
  칸마다 **같은 자리에 같은 모양**이 찍혀야 무늬로 읽힌다 (대나무 그림자에서 배운 것과 같다)
- **1,000점 프리미엄** (사용자가 정했다). 조선 세트에서 유일한 프리미엄이다
- **창틀 · 벽널까지 흑석으로 묶어봤다가 되돌렸다.** 벽널 `#5A6270` · 창틀 `#454D59` 로
  바닥보다 한 단씩 연하게 줘서 세트로 맞췄는데, 사용자가 **"창문이랑 벽지 아래쪽은 고풍스러운 갈색이 어울린다"** 고 했다.
  먹빛 띠 · 검은 전돌 사이에서 **나무 한 줄이 방을 덥혀주는** 쪽이 맞다.
  걸레받이도 먹빛일 때만 흑석으로 바꿨다가 같이 되돌렸다 — 지금은 벽지와 상관없이 한 가지 색이다

### 달밤 창호문 프리미엄 — 탈락 (9/21)

지금 것(30 x 30 · 700점 골드) 을 **38 x 38 · 1,200점 프리미엄**으로 키워 시안을 냈다가 접었다.
사용자 말 — **"뭔가 과하고 창문 느낌이 안 난다. 그냥 이전 게 낫다."** `drafts.ts` 는 다시 비었다.

- 얹었던 것 — 단청 상인방 · 용자살 · 청판 금 모란 · 놋쇠 문고리 · 머름. 장식을 더할수록 **창문이 아니라 문짝 · 장식장**이 됐다.
  지금 것이 창문으로 읽히는 이유는 **틀이 얇고 안(밤하늘 · 한지) 이 넓어서**다 — 키울 때 이 비율을 지켰어야 했다
- **한지에 비친 대나무 그림자는 두 번 그리고 뺐다.** 살 사이 종이 면이 일곱 칸 남짓이라
  잎을 어떻게 놓아도 살에 토막나서 대나무가 아니라 얼룩으로 보였다.
  38칸에서도 그랬으니, 그림자 같은 건 **살이 없는 넓은 면**에만 가능하다
- 다음에 다시 키운다면 — 장식은 위 띠 하나 정도만, 틀 두께는 지금처럼 두 칸,
  늘어난 칸은 전부 **밤하늘 쪽**에 주는 편이 낫겠다

### 프리미엄 후보 — 떨어진 것 · 남은 것

9/17 시안에서 떨어지거나 다시 그리기로 한 것. 지금 보고 있는 시안은 위 "시안실" 에서 연다.
넣은 여덟 개는 위 "프리미엄" 참고.

| 결과 | 아이템 | 이유 |
|---|---|---|
| 다시 | 샹들리에 | 더 고급스럽게. 2단 · 크리스털 줄 · 분홍 보석 · 반짝임을 제안해뒀다 |
| 탈락 | 피아노 | 어색하다 |
| 탈락 | 공주 드레스 | 판다 몸이 4줄뿐이라 드레스 느낌이 안 산다 — **입는 건 몸 밖으로 뻗는 모양이어야 산다** |
| 탈락 | 엄마 판다 쿠션 | 빼는 게 맞다 |
| 제안만 | 회전목마 오르골 · 왕실 망토 · 별빛 벽지 · 분홍 대리석 | 이번엔 안 골랐다 |
| 탈락 | 장미 격자 벽지 (골드) | 금 대각 격자 + 칸마다 장미 — **정신 사납다.** 벽지는 무늬가 방 전체에 깔려서 조용해야 한다 |
| **넣음** | 왕실 침대 1,000 (프리미엄) | 궁전 방과 한 벌. 35 x 34 — 금 기둥 위 진홍 휘장 지붕 · 보석 왕관 · 양옆 **비치는 망사 커튼**(한 칸 건너 칠함) · 금 테두리 **흰 이불**에 진홍 마름모. 첫 안은 진홍 이불이라 궁전 벽지에 묻혔다 |
| **넣음** | 금실 줄무늬 벽지 700 (골드) | 장미 격자 대신. 연분홍 넓은 띠 · 가장자리 금실 · 진주 알 · 아래 금줄 몰딩 |
| **넣음** | 꽃 타일 650 (골드) | 금 줄눈 크림 타일, 한 칸 건너 연분홍, 칸마다 분홍 꽃 |
| 뺐음 | 대리석 바닥 240 | 사용자가 빼자고 했다 |
| 탈락 | 장미 유리창 (창문 B안) | 생각보다 방에 안 어울린다 |
| **넣음 (전설 1,000)** | 발코니 창문 | 아주 마음에 들어했다. 38 x 38 로 궁전 창문보다 크다. 흰 프렌치 창 · 햇살 창살 · 금 발코니 난간, 창밖 노을 도시. 열기구 · 불빛이 움직인다 |
| **넣음 (전설 1,000)** | 궁전 창문 | 창문 A안. 발코니 창문처럼 38 x 38 로 키워 다시 그렸다 — 금 두 겹 틀 · 왕관 장식 · 주름 가림막 · 금술 커튼, 노을 해를 등진 동화 성 · 장미 정원, 깃발 · 새가 움직인다 |
| 탈락 | 왕실 초상화 (판다 공주 그림) | 일단 빼기로 했다 |
| **넣음** | 왕실 깃발 550 · 왕좌 850 · 유니콘 인형 600 · 장미 꽃병 500 | 왕궁 방에 어울리는 인형 · 장식. 유니콘은 판다보다 작게 줄였다 (18 → 13칸) |

9/14 코드 검토에서 나온 것들. 우선순위 순.

1. **배포** — `firebase.json`/`vercel.json` 없고 절차도 없다. 지금은 `npm run dev` 로만 쓴다.
   Vercel 권장 (Firebase Hosting 은 `next/image` 최적화가 죽는다).
   배포하면 환경변수 6개를 콘솔에 넣고, Firebase 승인된 도메인에 배포 주소를 추가해야 로그인이 된다
2. **README 가 현실과 다르다** — 없는 `mock.ts` 를 가리키고, "Firebase 아직 연결 안 함" 이라고 써 있다.
   `.env.local.example` 도 없어서 새 환경에서 클론하면 어떤 키가 필요한지 알 수 없다
3. **환경변수 가드 없음** — 값이 비어도 초기화가 통과하고 로그인 누를 때 정체불명 에러가 난다
4. **ESLint 재설정**
5. **시간표·학기를 Firestore 로** — 지금은 학기마다 코드 수정 + 재배포
6. **삭제 확인이 없다** — 일정·과제·단어 전부 빨간 버튼 누르면 즉시 사라지고 되돌릴 수 없다
7. **로그아웃이 헤더에 노출** — 확인도 없다. 설정 화면으로 옮기는 게 좋다
8. **날짜 유틸 테스트** — `weekOf`/`monthGrid`/`weekRange`. 제일 틀리기 쉬운데 검증이 없다
9. **`error.tsx` 없음** — 클라이언트 예외 하나면 프로덕션에서 흰 화면
10. **락파일 두 개** — `package-lock.json` 과 `pnpm-lock.yaml` 이 같이 커밋돼 있다
11. **무한 애니메이션에 `prefers-reduced-motion` 가드 없음** — 판다 쪽만 넣어뒀고 기존 5개는 그대로
12. **탭 이동하면 화면이 언마운트된다** — 토익 퀴즈 풀다 다른 탭 누르면 처음부터

### 가구 · 인형 방향 돌리기 (다음 할 일)

9/17 사용자와 정한 것. **상점 파일 나누기(위 "상점 파일") 는 이걸 위한 준비였고, 돌리기 자체는 아직 없다.**

- **대상은 가구 · 인형만.** 벽 장식(창문 · 액자 · 깃발) 은 벽에 붙어 방향이 없고, 옷은 판다가 입어서 해당 없다
- **그림을 90도 그대로 돌리는 게 아니다.** 정면에서 보는 방이라 돌리면 침대가 옆으로 쓰러진 모양이 된다.
  **바라보는 방향(앞 · 오른쪽 · 뒤 · 왼쪽) 을 바꾸는 것**이다
- **4방향을 다 그리지 않는다.** 앞 · 옆 · 뒤만 그리고 반대쪽 옆은 좌우 뒤집기(`flipRows`).
  어느 쪽에서 봐도 같은 것(러그 · 꽃병 · 어항) 은 한 장. 옆 · 뒤 그림이 없는 방향은 건너뛴다
- 지금 침대(`common/bed.ts`) 는 긴 쪽이 보이는 **옆모습**이다. 방향이 생기면 **머리를 뒷벽에 붙인 침대**(좁고 앞으로 긴 앞모습) 가 된다 —
  제일 흔한 배치라 가장 달라 보일 것

해야 할 것 — 이 순서로
1. ~~**시안**~~ 9/17 끝 — 시안실에서 **침대 하나로** 앞 · 옆 · 뒤를 그려 보여줬고 "일단 괜찮다" 로 2단계에 들어갔다
   - 순서는 **위에서 봐서 시계방향 앞 → 왼쪽 → 뒤 → 오른쪽** (`FACINGS`). 값이 없으면 그 아이템의 `face` = 지금 모양
   - 돌리면 **발끝 가운데를 그대로 두고** 새 그림을 세운 뒤 `fit()`. 왼쪽 위를 고정하면 침대가 옆으로 튄다
   - 앱 `PetRoom` 은 아직 방향을 모른다. 시안실은 `bed~front` 같은 가짜 아이템을 만들어 id 만 갈아 끼운다.
     누른 가구는 `onMove`(손 떼면 제자리여도 불린다) 로 안다. 화살표 자리 두 안(가구 옆 · 안내 띠) 을 버튼으로 바꿔 본다
   - 처음엔 **방에서 침대를 한 번 눌러야** 화살표가 떴는데, 사용자가 "뭘 눌러도 안 돌아간다" 고 했다 (눌러야 한다는 걸 몰랐고,
     캐노피 · 왕실 침대에는 방향 그림이 없다). 옮기기를 켜면 **바로 뜨게** 바꿨다. 앱에 넣을 때도 같은 함정이니 안내를 둘 것
   - 화살표는 **동그라미 버튼 없이 ↺ ↻ 모양만** (사용자가 버튼이 부자연스럽다고 했다). 벽 · 바닥 위에서 보이게 글자 둘레에 흰 테두리,
     누르는 자리는 30px 그대로. 안내 띠 안에서는 흰 글자
   - **화살표는 가구 옆으로 정했다** (9/17 사용자). 안내 띠 안은 시안실에서 뺐다
2. ~~**방향 그림**~~ 9/17 끝 — `Item` 에 `face` · `views`, 규칙은 `rules.ts` `viewsOf` · `viewAt` · `turn` · `canTurn`
   - **`sprite` 는 그대로 기본 방향 그림이다.** 침대 · 캐노피 · 왕실 침대 · 유니콘은 원래 옆모습이라 `face: "right"`, 나머지는 앞
   - `views` 에는 **오른쪽 · 뒤(· 앞)** 만 그린다. 왼쪽은 `viewsOf` 가 오른쪽을 뒤집어 채우고, 캐시에 담아 **늘 같은 객체**를 준다 (`Piece` memo)
   - 달린 것 11개 — 가구 8 (침대 · 캐노피 · 왕실 침대 · 책상 · 공부 책상 · 책장 · 화장대 · 왕좌) · 인형 3 (곰 · 토끼 · 유니콘).
     **둥근 탁자 · 스탠드는 어느 쪽에서 봐도 같아서 안 달았다** (`views` 가 없으면 화살표도 안 뜬다)
   - 책상은 뒤가 앞과 같지만 **뒤 그림을 같이 둔다** — 없으면 왼쪽 → 오른쪽으로 건너뛰어 반 바퀴가 한 번에 돈다
   - 침대 앞 · 뒤(20 x 21) 는 `common/bed.ts` — 캐노피가 그 위에 좁은 지붕 · 커튼을 얹어 쓴다 (20 x 31). 왕실 침대 앞 · 뒤는 25 x 32
   - 11개 그림이 자연스러운지는 사용자가 하나씩 눌러 보고 따로 말해주기로 했다
3. ~~**방향 저장**~~ 9/17 끝 — `Spot` (`lib/types.ts`) 에 `face?`. **없으면 그 아이템의 기본 방향** = 지금 모양
   - `PetRoom` 이 `lookOf(it, spot)` 로 그 방향 그림을 골라 그린다. **크기 · 발끝 정렬 · 집는 자리 · `fit()` 이 전부 그 그림을 따른다**
     (`fit` 에 그림을 넘기는 인자가 생겼다. 안 넘기면 기본 그림)
   - 돌리기는 `turnSpot(it, spot, dir)` (`PetRoom.tsx`, `fit` 옆) — 발끝 가운데 고정 · 두 칸 격자 · `fit()` 까지 해서 새 spot 을 돌려준다. 5단계(방 밖으로 못 나가게) 도 여기서 끝났다
   - **움직이는 가구(자개장) 는 기본 방향일 때 `spriteAt` 을 쓴다** — 방향 그림은 한 장이라, 한 바퀴 돌아 앞으로 와도 `viewAt` 을 쓰면 반짝임이 멈췄다
   - **돌릴 때는 두 칸 격자에 안 붙인다.** 붙였더니 폭 차이 반올림이 쌓여 한 바퀴 돌리면 4~6칸 밀려 있었다.
     반 칸이 남으면 커질 때 내리고 작아질 때 올려서 갔다 오면 제자리 (벽에 막혀 `fit` 이 민 것은 어쩔 수 없다)
   - **옮길 때 `{ ...s, x, y }` 로 쓴다.** 전에는 `{ id, x, y }` 로 새로 만들어서, 그대로 두면 옮길 때마다 돌린 방향이 풀렸다
   - **Firestore 는 `undefined` 를 못 쓴다** — `face` 는 돌렸을 때만 붙고, 새로 꺼낸 가구(`withItem`) 에는 키 자체가 없다
   - 시안실도 가짜 아이템(`bed~front`) 을 버리고 **앱과 같은 `face` · `turnSpot`** 으로 돈다
4. ~~**화살표**~~ 9/17 끝 — `components/TurnArrows.tsx`. 옮기기 중 **가구 옆에** ↺ ↻ (동그라미 없이 모양만, CSS `.pet-turn`)
   - 어느 가구에 띄울지는 `turnTarget` — 마지막으로 누른 게 돌릴 수 있으면 그것, 아니면 방의 첫 돌릴 수 있는 가구.
     누른 가구는 `PetView` `moveItem` 에서 안다 (제자리에서 눌렀다 떼도 불린다)
   - 돌리면 `turnItem` 이 `turnSpot` 결과를 `save` 한 번으로 쓴다 (방향 · 자리 같이)
   - 벽 가까이에선 화살표를 방 안으로 밀어 넣는다 (가구를 조금 가린다). 안내 띠에 "침대 · 앞" 처럼 지금 방향을 같이 적는다
   - **시안실도 같은 `TurnArrows` · `turnSpot` 을 쓴다** — 시안실에서 되면 앱도 된다. 로그인이 필요해서 앱 화면은 직접 못 눌러봤다
5. ~~**방 밖으로 못 나가게**~~ 3단계 `turnSpot` 에서 같이 끝났다

벽지 · 바닥 무늬를 `PetRoom` · `PetView` 두 군데서 그리는 건 **일단 그대로 둔다** (9/17 사용자)

### 토익 고도화 (보류)

방향만 잡아둔 상태. `Word` 에 학습 이력(`correctCount`/`wrongCount`/`lastSeenAt`)이 없어서
오답노트도 복습 주기도 못 만든다. 그 필드 세 개가 모든 것의 전제.
지금은 매번 전체에서 무작위 5개라 **모르는 단어가 계속 모르는 채로 남는다.**

---

## 작업 일지

### 2026-09-23
- **윷놀이 손질 일곱 가지** (사용자가 "고도화할 게 남았나" 물어 훑고 고른 것). 위 "윷놀이 이벤트" 참고.
  - **던질 빚이 겹치면 하나가 사라지던 버그** — `pending` 참/거짓을 `owe` 숫자로. 위 "판 한 번의 흐름"
  - **판 초기화** · **갈림길 고르기** · **말이 한 칸씩 걸어가기** · **밭 이름** · **윷가락 좌우 흩어짐**
  - **현판의 기간 · D-day 제거** (`config.ts` 의 `EVENT` 도 같이 지웠다)
  - `tools/yut-check.mts` 를 **59 → 81가지**로 늘렸다 (갈림길 · 걸음길).
    갈림길을 넣으면서 `advance` 에 `branch` 인자가 생겼는데, **길을 두 군데서 셈하지 않도록**
    `advance` 와 `pathOf` 를 `run()` 하나에서 뽑았다
  - **점수판의 골인 표시를 체크로** — 난 말과 안 나간 말이 구분이 안 됐다 (사용자 지적). 위 "화면"
  - **아직 안 한 것** — 잡기 · 나기 연출(아이디어를 냈지만 **지금 상태로 두기로 했다** — 9/23 사용자),
    무르기, 시안 페이지 옮겨심기

### 2026-09-22
- **윷놀이 말길 · 규칙을 실제 윷놀이에 맞췄다** (사용자 지적에서 시작 · 대회규정 확인).
  길 방향이 좌우로 뒤집혀 있었고, 참먹이에 서기만 해도 나 버렸고, 도에서 백도면 한 바퀴를 다시 돌았다.
  위 "길 — 밭 번호가 곧 규칙이다" 참고. **검사는 34 → 59가지**로 늘렸다
  - 같은 밭에 이름을 여럿 준 게 이번 고침의 핵심이다 — 백도가 "온 길" 로 되물리려면
    그 밭에 **어느 길로 들어왔는지**를 들고 있어야 한다. 대신 잡기 · 업기가 어긋나지 않게
    `field()` 로 다시 한 밭으로 모은다. 한쪽만 고치면 **같은 자리에 서 있는데 안 잡히는** 판이 된다
  - 이벤트가 도는 중이라 **골 번호(20) 는 그대로 두고** 참먹이에 새 번호(30~32) 를 줬다
- **던진 윷 칸 개편** (사용자 요청). 이번 차례 것만 담고 차례가 넘어가면 비운다 —
  둘이 한 폰으로 번갈아 던지니 기록이 섞여 보기 어려웠다. 머리줄에 이름, 패에 그 편 과일 색
- **윷 · 모는 모아 던지고 나서 고른다** (사용자 요청). 옮기지 않고 계속 던질 수 있고,
  나온 값들 중에 골라 옮긴다. 두 버튼이 `.ev-acts` 한 줄에 나란히 선다
- 셋 다 시안 페이지(버전 13) 에도 옮겨 심었다
- **이벤트 잠금을 가이드에서 여닫는다** (사용자 요청). 위 "잠금 — 가이드에서 몰래 여닫는다" 참고.
  어제 넣은 `TabBar` 의 `LOCKED` 배열을 Firestore(`event/lock`) 로 옮긴 것이고,
  **자물쇠 모양과 "울 아가 혼나요" 팝업은 그대로 뒀다**
  - 시안을 먼저 보였다 (숨긴 칸 · 비밀번호 창 · 탭을 숨길지 자물쇠로 둘지):
    **https://claude.ai/artifact/J3zpqBMo3z92yH6YWQ7t1B** (윷놀이 시안과 다른 페이지다)
  - **작업하는 동안 원격에 9/21 잠금 커밋이 올라와 있는 걸 몰랐다.** 로컬이 `3507d06` 에 멈춰 있어서
    같은 것을 다시 만들었다가 원격 것에 맞춰 도로 얹었다. **손대기 전에 `git fetch` 부터 할 것**

### 2026-09-21
- **이벤트 탭 잠금** (사용자 요청). 탭은 그대로 보이고 아이콘에 작은 자물쇠가 붙는다.
  누르면 탭이 안 바뀌고 "울 아가 혼나요" 팝업만 뜬다.
  **잠금 여부는 `TabBar` 의 `LOCKED` 배열 하나**다 — 다시 열어줄 때 거기서 "event" 만 빼면 된다.
  이벤트 화면/데이터는 손대지 않았다 (자물쇠를 풀면 하던 판이 그대로 이어진다)
- **로딩 화면에 당근 게이지** (사용자 요청 · 시안 셋 중 "당근 한 개" 로 골랐다).
  토끼 · "잠깐만" 아래에서 주황 당근이 왼쪽부터 찬다
  - **`Splash.tsx` 를 새로 뒀다.** `page.tsx` 의 dynamic 로딩 화면과 `AppRoot` 의 로그인 확인 화면이
    같은 화면을 복붙해 두고 있어서 한 군데로 모았다.
    **Splash 는 Firebase 를 건드리는 것을 하나도 import 하지 않는다** — `page.tsx` 가 `ssr:false` 로 두는 이유와 같다
  - 당근은 도트 한 장(`CARROT_ROWS`)을 **두 벌로 나눠** 쓴다 — 빈 속(`CARROT_EMPTY`) 을 깔고
    찬 속(`CARROT_FILL`) 을 덮은 뒤 `clipPath` 를 왼쪽부터 넓힌다.
    테두리와 꼭지가 바탕에만 있어서 **다 차기 전에도 당근 모양이 다 보인다**
  - 자르는 폭 **6 → 38 이 몸통 32칸**이라 `steps(32)` 로 한 칸씩 또렷하게 찬다 (globals.css 의 `carrot-fill`).
    도트 폭을 바꾸면 그 두 숫자도 같이 고칠 것
  - **진행률은 가짜다.** 얼마나 남았는지 알 수 없어서 다 차면 도로 비우고 되풀이한다.
    대개는 다 차기 전에 앱이 떠서 사라진다
  - 겹쳐 그리려고 `PixelSprite` 에서 도트 찍는 부분만 `spriteCells` 로 빼서 내보낸다
- **"잠깐만" 이 다시 튄다.** 글자를 `span` 셋으로 쪼개 둔 채 키프레임만 없어져 있었다
  (업데이트 내역에는 튄다고 적혀 있었다 — 언제 빠졌는지는 모른다)
- **윷놀이 이벤트 탭** (7일짜리). 위 "윷놀이 이벤트" 참고.
  UI 를 먼저 시안으로 네 번 고쳐 보이고(판 → 던지기 애니메이션 → 딸기 · 망고 말 → 한국 색) 그다음 앱에 넣었다.
  **규칙은 화면 없이 `tools/yut-check.mts` 로 확인했다** — 지름길에서 바로 나 버리던 것을 여기서 잡았다
- **강의 탭에 `할일` 종류를 더했다** (사용자 요청). 강의 · 과제와 나란히 세 번째 칸.
  - **할일에는 과목이 없다** — 수업과 상관없는 일을 적는 칸이라 팝업에서 과목 줄을 아예 숨기고 `courseId` 를 빈 문자열로 저장한다.
    그래서 `courseOf` 가 **못 찾으면 첫 과목이 아니라 `null`** 을 돌려준다 (전에는 `?? COURSES[0]`).
    목록에서는 과목 이름 없이 날짜만 보이고, 종류 딱지는 과목 색 대신 `--band` 분홍이다.
    덤으로 **학기가 바뀌어 과목 id 가 없어진 옛 항목**도 엉뚱한 과목에 붙지 않고 과목 없이 보인다
  - 종류 목록은 `LectureView` 맨 위 `KINDS` 하나다. 주차 안에서 묶는 차례와 팝업 버튼 차례가 여기서 같이 나온다 —
    두 군데에 배열을 적어두면 한쪽만 고쳐서 어긋난다
  - **캘린더 말풍선은 이제 종류를 안 가린다** (`AppRoot`). 같은 날 **강의만 → 강의 · 할일 → 전부** 로 두 번 넓혔다 (둘 다 사용자 요청).
    "과제는 기한이 길어서 섞으면 뜻이 흐려진다" 고 빼뒀던 것인데, 쓰는 사람은 **이번 주에 안 끝낸 것을 한 숫자로** 보고 싶어 했다.
    조건에 `kind` 가 아예 없으므로 종류가 또 늘어도 따라온다.
    말풍선 글은 "안 들은 강의" → "강의 · 과제 · 할일" (`CalendarView`)
  - 포인트는 강의 · 과제와 같은 한도(`TASK_CAP` 하루 다섯 번) 를 같이 쓴다. 종류를 늘려도 하루 벌이는 안 는다
- **먹빛 전돌 바닥** 추가 (1,000 · 프리미엄). 먹빛 벽지와 한 벌. 위 "먹빛 전돌 바닥" 참고
- **가구를 한 칸씩 못 옮기던 것.** 놓을 때 두 칸 격자에 붙이느라 홀수 칸이 통째로 막혀 있었다.
  사용자가 창문을 뒷벽 가운데쯤에서 한 칸 밀어 보고 "다시 돌아온다" 고 알려줬다. 격자를 없앴다 (`SNAP` 삭제)
- **단청 한지 벽지 → 먹빛 한지 벽지.** 띠만 갈았다. 위 "먹빛 한지 벽지" 참고
- **상점 · 옮기기 버튼을 방 밖으로.** 방 위에 뜬 버튼이 앞바닥을 가려 그 자리에 가구를 못 놓았다.
  스티커 / 키우기 를 나누던 줄을 **조작줄(스티커 · 상점 · 옮기기)** 로 바꾸고 스티커는 큰 팝업으로 (사용자가 짠 안이다).
  세로 공간이 안 늘어나는 게 이 안의 핵심이다 — 방 아래에 줄을 더하면 "한 화면" 제약에 걸린다.
  `PetBar.tsx` 새로 · `PandaView` 는 껍데기에서 **탭 주인**이 됐고 `PetView` 는 방 + 상점만 맡는다.
  시안실도 같은 조작줄을 쓰고, **폰 화면 시안**을 넣어 폰에서 먼저 보게 했다 (위 "시안실" 참고)
- **가구가 벽에서 떨어져 서던 것.** 뒤로 밀 수 있는 한계가 `floorTop - 그림높이/2` 라
  **키가 클수록 벽에서 멀리** 섰다 — 침대 옆모습 6칸 · 공주 화장대 11칸 · 자개장 13칸 · 왕실 침대 17칸(68px).
  침대는 낮아서 붙은 듯 보이고 화장대는 멀찍이 떠 보이던 게 이것이다.
  한계를 **발끝이 벽선에 닿는 줄**(`floorTop - h`) 로 바꿔서 키와 상관없이 붙는다.
  발끝이 벽선이면 맨 아랫줄이 걸레받이 줄을 덮어 벽에 붙어 선 모습이 된다.
  `flat`(러그 · 방석) 은 반대로 `floorTop` 아래로 묶었다 — 예전엔 절반이 벽에 걸릴 수 있었다.
  저장된 자리는 그릴 때 `fit()` 을 다시 타므로, 벽 위로 올라가 있던 러그는 그만큼(최대 절반) 내려온다.
  **눈으로 확인한 방법** — `sucrase` 로 `PetRoom` 을 서버 렌더해 SVG 를 뽑고,
  `<rect>` 만 긁어 PNG 로 그려 전후를 견줬다 (앱을 띄우지 않고 방을 보는 방법이다)
- **나머지 두 칸도 같은 선으로.** "다른 것도 전부" 라는 말을 듣고 네 칸을 다 세 봤다 (68장, 방향 그림까지).
  - `top`(화분 · 책 · 꽃병) 은 **위 고치기 때문에 새로 어긋난 것이다.** 한계가 "걸레받이 위 14칸" 고정이라
    가구가 벽까지 가게 되자 자개장 윗면보다 11칸 아래에서 막혔다 — 벽에 붙인 장 위에 화분을 못 얹는다.
    제일 큰 가구 높이를 **그림에서 재서**(`topReach()`) 그 윗면까지 열었다. 고정값이면 더 큰 가구가 들어올 때 또 어긋난다
  - `wall`(창문 · 액자) 은 아래로 `base - h - 1` 에서 멈춰 **걸레받이에 두 줄(8px) 못 미쳤다.**
    달밤 창호문처럼 바닥까지 내려와야 하는 것이 떠 보여서 `floorTop - h` 로 맞췄다
  - 확인은 `fit()` 을 직접 불러서 — 모든 아이템 · 모든 방향을 끝까지 올리고 내려 벽선과의 틈을 셌다. 지금은 전부 0

### 2026-09-17
- **조선 가구 넷(병풍 · 자개장 · 반닫이 · 소반) 돌리기 그림.** 돌린 뒤 자리가 밀리던 것 · 자개장 반짝임이 멈추던 것도 고쳤다
- **조선 세트 13종을 상점에 넣었다** (골드 10 · 일반 3, 7,800점). 상점 26,900점. 위 "조선 세트" 참고
- **가구 · 인형 돌리기.** 옮기기 중 가구 옆 ↺ ↻ 로 90도씩. 가구 8 · 인형 3 에 앞 · 옆 · 뒤 그림, 방향은 자리(`spots`) 의 `face` 에 저장.
  시안(침대 하나) → 그림 11개 → 저장 → 화살표 순서로 했다. 위 "가구 · 인형 방향 돌리기" 참고
- **상점을 `src/shop/` 으로 나눴다.** 아이템 하나 = 파일 하나, 칸마다 폴더, 같이 쓰는 부품은 `common/`.
  가구 · 인형 방향 돌리기(옮기기 중 가구를 누르면 좌우 화살표로 90도씩) 를 넣기 위한 준비다 — 아직 돌리기는 없다.
  그림 · 값 · 순서는 하나도 안 바뀌었다. 위 "상점 파일" 참고
- **시안실은 처음 열면 빈 방.** 민무늬 벽 · 맨바닥 · 가구도 옷도 없이 연다 (사용자 요청). 새 아이템을 하나씩 놓아보는 곳이라
  왕궁 방을 미리 채워두면 오히려 가렸다. "처음 방으로" 버튼은 "방 비우기" 와 같아져서 뺐다.
  저장 키를 `pet-preview:5` 로 올려서 전에 열어본 사람도 빈 방으로 열린다
- **캐노피 침대 900 → 750**, 그 위 프리미엄 침대로 **왕실 침대(1,000)** 를 넣었다 — 네 기둥 · 진홍 휘장 지붕 · 비치는 망사 커튼 · 흰 이불.
  **망사는 한 칸 건너 칠해서 뒤가 비치게** 했다. 진한 색 이불은 궁전 벽지 진홍에 묻혀서 흰색으로 갔다
- **궁전 벽지를 왕실 카펫과 한 벌로** — 크림 바탕 금 테두리 마름모, 위 진홍 띠, 아래 진홍 벽널 금테 칸. 금실 줄무늬 벽지도 넣었다
- **상점 등급 3단** — 값으로 정한다: 일반 ~499 · 골드 500~999(금테) · 프리미엄 1,000~(보라). 궁전 벽지 · 왕실 카펫을 1,000 으로 올려 프리미엄에.
  대리석 바닥을 뺐고, 골드 벽지 · 바닥이 없어서 시안을 만들었다 — **꽃 타일은 넣고**, 장미 격자 벽지는 정신 사나워서 금실 줄무늬로 다시 그렸다. 위 "상점 등급" 참고
- **상점 비싼 순서** — 칸마다 값이 높은 것부터 보이게 했다
- **토익 퀴즈를 문제당 10점으로** — 만점에만 50 주던 걸 맞힌 개수 x 10 으로. 하루 첫 판만 치고 0개여도 기회를 쓴다 (사용자 요청). 하루 최대는 50 그대로
- **스티커 포인트 올리기** — 스티커 25→100, 10개 보너스 100→500 (사용자 요청). 연속 +50 은 그대로. 부지런한 날 약 300점.
  셈이 "차이" 라 이미 붙인 스티커에 소급해 주지는 않는다 — 다음 스티커부터 새 값
- **포인트 올리기** — 부지런한 날 180점이 되게 스티커 15→25 · 퀴즈 30→50 · 타이머 10→15. 체크는 20 그대로. 위 포인트 표 참고
- **왕궁 세트 · 전설 등급** — 창문 둘을 1,000점 전설로(보라 칸), 깃발 · 왕좌 · 유니콘 · 꽃병을 프리미엄으로 상점에 넣었다. 시안실 `drafts.ts` 는 비었다
- **왕궁 장식 시안** — 장미 유리창을 빼고, 궁전 창문과 어울리는 초상화 · 깃발 · 왕좌 · 유니콘 인형 · 장미 꽃병을 시안실에 올렸다
- **시안실** — 시안마다 새 페이지를 만들던 걸 그만두고 키우기 전용 페이지 하나로 모았다. 위 "시안실" 참고
- **프리미엄 여덟 개 추가** (5,350점어치). 시안 두 번 — 1차 여덟 개 중 넷 채택, 2차 넷 전부 채택.
  움직이는 소품(`anim`) · 등 자리(`back`) · 새 무늬 둘(`palace` · `royal`) · 금테 상점 칸이 같이 들어갔다.
  위 "프리미엄" 참고
- **공(40) 을 뺐다.** 살 일이 없을 것 같다는 말을 들었다. 상점 26점 · 6,490점, 제일 싼 게 책 더미(50).
  이미 산 사람 문서에 `ball` 이 남아 있어도 `itemById` 가 못 찾아 방에서는 조용히 사라진다.
  **환불은 코드에 없다** — 산 적이 있으면 콘솔에서 `earned` 에 40 을 더해줄 것
- **상점 칸 4 → 7.** 기타에 열여섯 개가 몰려 있어서 나눴다. 두 줄로 — 윗줄 **꺼내놓는 물건**
  (옷 4 · 가구 6 · 인형 2 · 소품 5), 아랫줄 **방 자체** (벽 장식 9 · 벽지 8 · 바닥 8).
  `타일` 칸은 `바닥` 으로 이름을 바꿨다 — 잔디·대리석까지 들어 있어서 타일이 아니다.
  창문은 `win()` 이 칸을 정하므로 새 창문도 저절로 벽 장식에 들어간다.
  **새 소품을 만들면 `cat` 을 이 일곱 중에서 고를 것** (`CAT_ROWS`). 인형 칸은 곰·토끼 둘뿐이다 (공은 뺐다)
- **가구 옮기기 버그.** 창문을 옮기고 끝낸 뒤, 다시 옮기기로 다른 가구를 잡으면 창문이 처음 자리로 돌아갔다.
  원인은 아이폰의 `pointercancel` 로 저장이 빠진 것 — 위 "넘어졌던 곳" 참고
- **침대 · 책상 폭 늘리기** (너무 좁다는 말을 들었다). 침대 22 → 30 (베개는 그대로, 이불만),
  책상 12 → 20, 공부 책상 14 → 22 (서랍 칸은 그대로, 다리 사이만). 높이는 안 바꿨다.
  책상 처음 자리는 오른쪽 끝에 걸려서 x 56 → 52.
  **넓어진 그림이 벽 밖으로 삐져나오지 않게, 저장된 자리도 그릴 때 `fit()` 으로 한 번 더 가둔다**
  (저장값은 안 고친다). 그림 크기를 바꿀 일이 또 생겨도 이것 때문에 안전하다
- **가이드 팝업** 추가. 헤더 왼쪽 위 작은 버튼 → 포인트 얻기 / 업데이트 내역.
  업데이트 내역은 `lib/updates.ts` 에 사용자 말로 날짜마다 한 칸씩. 위 "가이드 · 업데이트 내역" 참고
- **포인트 새는 곳 막기.** "체크하면 주고 풀면 뺀다" 는 이미 돼 있었다. 실제로 샌 곳은 셋 —
  ① 화면이 갱신되기 전에 두 번 누르면 두 번 줬다 (중복 체크) → 트랜잭션으로 이미 그 상태면 무시
  ② 빈 과제를 만들어 체크하면 끝없이 받았다 → **하루 다섯 번** 한도 (`TASK_CAP`)
  ③ 과제를 고쳐 저장할 때 옛날 `done` 으로 덮을 수 있었다 → `save` 는 `done` 을 안 쓴다.
  스티커는 오늘 칸만 되고 차이로 계산해서 안 샜다. 잔액을 0 으로 가리던 걸 음수 그대로 보여주게 했다 —
  사고 나서 풀면 "안 깎인 것" 처럼 보여서 새는 줄 알았던 것이다.
  체크는 이제 서버를 다녀와야 반영돼서, 그동안은 누른 모양을 먼저 보여주고 또 누르는 건 받지 않는다
- **구매는 아직 트랜잭션이 아니다.** `spent` 를 화면의 `pet` 에서 더한 값으로 덮어쓴다.
  확인창이 있어 연타는 어렵지만, 막으려면 구매도 트랜잭션으로 옮길 것
- **history.md 를 코드와 맞췄다.** 포인트 표에 상수 이름·되돌아가는 경우를 붙이고,
  옛날 값이던 "강의·과제 30점"(지금 20) · "제일 비싼 게 300점"(지금 320) 을 고쳤다.
  상점 총액 6,530 · 40~320 은 `ITEMS`/`WALLS`/`FLOORS` 를 직접 더해서 확인했다.
  **하루 속도를 다시 세 보니 "넉 달" 이 아니라 두 달 남짓**이라 정해진 규칙에 적어뒀다 (값은 아직 안 만졌다)

### 2026-09-16
- **포인트 개편.** 스티커 20→15, 체크 30→20 으로 내리고 **토익 퀴즈 만점(30, 하루 한 번)**,
  **타이머 25분(10, 하루 네 번)**, **스티커 7일 연속(+50)** 을 새로 뒀다.
  총량은 비슷한데 하는 일마다 값이 붙는다. 위 "정해진 규칙" 표 참고
- **가구 열한 개 · 벽지 타일 넷** 추가 (2,550점어치). 가구는 전부 **흰색**으로 맞추고
  색은 이불·책·화분 같은 작은 것에만 남겼다. 흰 가구는 밝은 벽지 위에서 묻혀서
  **실루엣 둘레를 테두리색으로 한 겹 두른다** — 이게 이 그림들의 전제다.
  러그는 20 x 5 에서 **28 x 8** 로 키웠다 (판다가 올라서도 자리가 남아야 깔개로 보인다)
- **키우기 화면을 통째로 고쳤다.** 방 48 x 57 → **80 x 92 세 면**, 판다가 바닥을 스스로 돌아다니고,
  가구를 끌어서 배치한다. 새 소품 셋 — 분홍 러그(100) · 공(40) · 액자(120).
  시안을 네 번 고쳐서 확정했다: 삼면 세우기 → 옆벽도 같은 색(꺾인 자리 한 줄만 어둡게) →
  칸 크기 견주기(80 유지). 위 "방 — 세 면" 항목 참고
- **강의·과제 체크에 30점** (같은 날 포인트 개편에서 20점으로 내림). 체크를 풀거나, 체크된 것을 지우면 그만큼 회수한다
- **상점에 확인창.** 살 때 "○○을 구매하시겠습니까?"(확인/취소), 모자라면 "N포인트가 모자랍니다".
  상점 팝업 위에 팝업을 하나 더 얹는 식이다 (뒤에 있는 `.dim` 이 위로 온다)
- **기념 선물 500점.** Firebase 콘솔에서 `pet/state` 의 `earned` 에 직접 더했다.
  **스티커 개수와 점수가 안 맞는 건 이것 때문이다.** 코드에는 선물 기능이 없다
- **타이머** 추가. 처음엔 아래 탭 여섯 번째로 뒀다가 번잡해서 **시간표 탭 안**으로 옮겼다.
  수업 시간(시간표)·공부 시간(타이머) 으로 결이 같고, 시간표가 불러올 게 없는 정적 화면이라 자리가 남았다.
- 시계 모양은 **시안을 만들어 보여주고 네 번 고쳐서** 확정했다 —
  도트 → 매끈한 획, 잔상 빼기, 버튼 입체 그림자 빼기, 버튼을 아래로 내리기.
  폰에서 눕혀야 보이는 화면이라 시안을 먼저 보는 쪽이 빨랐다. 위 "타이머" 항목 참고

### 2026-09-15
- **판다 키우기** 추가. 스티커 탭 → 판다 탭 (스티커 / 키우기). 시안을 13차까지 돌려 확정
- 포인트를 스티커에서 매번 세던 것을 **저장 방식으로** 변경.
  덤으로 "0에서 시작" 이 제대로 지켜졌다 (전에는 기존 스티커를 소급해서 세고 있었다)
- 상점 칸을 네 상태로 구분 — 안 산 것(회색 + 자물쇠) · 가진 것 · 장착 중 · 기본
- 방 높이 68 → 57 줄 (바닥만 절반). 사파리에서 스크롤해야 상점 버튼이 보이던 문제
- 판다 16x19 → **16x16** (머리 12폭, 눈 2줄). 판다 자리도 x 16 → 10 으로 옮겨
  오른쪽 끝까지 걸어도 책상을 다 가리지 않게 했다
- **큰 창문 7종** (아침·낮·저녁·눈·벚꽃·여름·꽃밭). 24x22 로 판다 뒤에 걸린다.
  틀은 공통이고 하늘색·땅색·부품만 갈아 끼우는 구조라 종류를 늘리기 쉽다
- 유치원 모자 새 모양(갓처럼 보여서), 왕리본 2색(분홍·하늘)으로 교체

### 2026-09-14
- 캘린더 일정 4개부터 `+N` 으로 접기
- 시간표 시각 수정 (`Course.time` → `start`/`end`)
- **시간표 탭 신설** + 캠퍼스 지도. 격자 대신 요일별 목록으로 간 이유는
  폰 폭에서 격자 한 칸이 50px 이라 강의실 이름이 안 들어가서
- 지도 로딩 개선 — 정적 import + `placeholder="blur"` + `sizes` 조정 (1.6MB → 57KB)
- `displayWeek` 을 `date.ts` 로 빼서 캘린더와 강의 탭이 같은 숫자를 세게 수정
- 캘린더 말풍선은 강의만 세기 (과제 제외)
- 판다 도장 웃는 얼굴로 (입이 ∧ 라 울상이었다)
